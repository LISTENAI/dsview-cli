---
phase: 15-decode-config-model-and-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/dsview-sys/wrapper.h
  - crates/dsview-sys/bridge_runtime.c
  - crates/dsview-sys/src/lib.rs
  - crates/dsview-sys/tests/boundary.rs
  - crates/dsview-core/src/lib.rs
  - crates/dsview-core/tests/decode_config.rs
autonomous: true
requirements:
  - DEC-03
must_haves:
  truths:
    - "Phase 15 defines a JSON decode config model with one root decoder, numeric channel bindings, and an ordered linear stack."
    - "Decode option metadata exposes enough type information to validate typed JSON values without inventing new decoder or option ids."
    - "The Rust config model is shaped so Phase 16 can consume it directly for offline decode execution."
  artifacts:
    - path: crates/dsview-core/src/lib.rs
      provides: "Typed JSON decode config schema and normalized decode config domain types"
      contains: "pub struct DecodeConfig"
    - path: crates/dsview-sys/src/lib.rs
      provides: "Decode option metadata with explicit value-kind information"
      contains: "pub enum DecodeOptionValueKind"
    - path: crates/dsview-core/tests/decode_config.rs
      provides: "Schema and normalization coverage for valid JSON config inputs"
      contains: "parse_decode_config_with_linear_stack"
  key_links:
    - from: crates/dsview-sys/src/lib.rs
      to: crates/dsview-core/src/lib.rs
      via: "typed decoder option metadata flows into the config schema without remapping canonical ids"
      pattern: "DecodeOptionValueKind|DecodeConfig"
---

<objective>
Define the typed decode config schema and the supporting option-type metadata needed to represent root decoder, channel bindings, typed options, and a linear stack in Rust.

Purpose: Phase 15 must create the JSON config contract before validation can be added. The config model should preserve canonical upstream ids, use numeric channel indexes, and expose a stable internal representation that later phases can execute directly.
Output: Typed config structs, serde parsing/normalization, and explicit option value-kind metadata sourced from decoder discovery.
</objective>

<execution_context>
@/home/seasonyuu/.codex/get-shit-done/workflows/execute-plan.md
@/home/seasonyuu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-decode-config-model-and-validation/15-CONTEXT.md
@.planning/phases/15-decode-config-model-and-validation/15-RESEARCH.md
@.planning/phases/15-decode-config-model-and-validation/15-VALIDATION.md
@.planning/phases/14-decode-runtime-boundary-and-decoder-registry/14-CONTEXT.md
@.planning/phases/14-decode-runtime-boundary-and-decoder-registry/14-RESEARCH.md
@.planning/phases/14-decode-runtime-boundary-and-decoder-registry/14-VERIFICATION.md
@CLAUDE.md
@crates/dsview-sys/wrapper.h
@crates/dsview-sys/bridge_runtime.c
@crates/dsview-sys/src/lib.rs
@crates/dsview-sys/tests/boundary.rs
@crates/dsview-core/src/lib.rs
@DSView/DSView/pv/storesession.cpp
@DSView/libsigrokdecode4DSL/libsigrokdecode.h
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend decode option metadata with explicit value kinds</name>
  <files>crates/dsview-sys/wrapper.h, crates/dsview-sys/bridge_runtime.c, crates/dsview-sys/src/lib.rs, crates/dsview-sys/tests/boundary.rs</files>
  <read_first>
    - .planning/phases/15-decode-config-model-and-validation/15-CONTEXT.md
    - .planning/phases/15-decode-config-model-and-validation/15-RESEARCH.md
    - crates/dsview-sys/wrapper.h
    - crates/dsview-sys/bridge_runtime.c
    - crates/dsview-sys/src/lib.rs
    - crates/dsview-sys/tests/boundary.rs
    - DSView/libsigrokdecode4DSL/decoder.c
    - DSView/libsigrokdecode4DSL/util.c
  </read_first>
  <behavior>
    - Decode option metadata exposes an explicit value-kind derived from upstream option defaults/allowed values.
    - Canonical upstream option ids remain unchanged.
    - Allowed values remain inspectable after conversion so the validator can check typed JSON values deterministically.
  </behavior>
  <action>In `crates/dsview-sys/wrapper.h`, add a C enum for decode option value kinds with explicit variants for string, integer, float, and unknown. Extend the raw decode option snapshot with a `value_kind` field while preserving the existing canonical `id`, `default_value`, and `values` fields. In `crates/dsview-sys/bridge_runtime.c`, derive `value_kind` from the upstream `GVariant` default in the decoder option metadata, mapping string defaults to string, integer-family defaults (`BYTE`, `INT16`, `UINT16`, `INT32`, `UINT32`, `INT64`, `UINT64`) to integer, and double defaults to float. In `crates/dsview-sys/src/lib.rs`, add `DecodeOptionValueKind` and expose it on `DecodeOption`. In `crates/dsview-sys/tests/boundary.rs`, add coverage named `decode_option_value_kind_follows_upstream_default_type` that proves `address_format` remains string-like and that integer/float fixtures map correctly without changing canonical ids.</action>
  <acceptance_criteria>
    - `rg -n "enum dsview_decode_option_value_kind|value_kind" crates/dsview-sys/wrapper.h crates/dsview-sys/bridge_runtime.c` finds the raw option-kind additions.
    - `rg -n "pub enum DecodeOptionValueKind|pub value_kind: DecodeOptionValueKind" crates/dsview-sys/src/lib.rs` finds the safe Rust metadata shape.
    - `rg -n "decode_option_value_kind_follows_upstream_default_type" crates/dsview-sys/tests/boundary.rs` finds the new boundary regression.
    - `! rg -n "stable_id|token" crates/dsview-sys/src/lib.rs crates/dsview-sys/wrapper.h` succeeds, proving no new alias namespace was introduced.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p dsview-sys --test boundary -- --nocapture</automated>
  </verify>
  <done>Decoder option metadata is now typed strongly enough for JSON config validation without changing canonical upstream ids.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define and parse the JSON decode config model in core</name>
  <files>crates/dsview-core/src/lib.rs, crates/dsview-core/tests/decode_config.rs</files>
  <read_first>
    - .planning/phases/15-decode-config-model-and-validation/15-CONTEXT.md
    - .planning/phases/15-decode-config-model-and-validation/15-RESEARCH.md
    - crates/dsview-core/src/lib.rs
    - crates/dsview-sys/src/lib.rs
    - crates/dsview-cli/src/lib.rs
    - DSView/DSView/pv/storesession.cpp
  </read_first>
  <behavior>
    - Core exposes a JSON-first config model with one root decoder plus a linear stack.
    - Channel bindings are numeric capture-channel indexes keyed by canonical upstream channel ids.
    - Option values are represented as a typed enum rather than forced strings.
  </behavior>
  <action>In `crates/dsview-core/src/lib.rs`, add a `DecodeConfig` domain with serde-enabled structs that represent: an optional or defaulted schema version, a root `decoder` object containing `id`, `channels`, and `options`, and a `stack` array of downstream decoder entries containing `id` and `options`. Add a typed `DecodeOptionValue` enum that supports string, integer, and float JSON values, and keep the field names machine-friendly (`decoder`, `channels`, `options`, `stack`) rather than copying DSView's array-of-one-key GUI persistence shape. Add parsing/normalization helpers that produce a stable internal config model for later validation and execution. In `crates/dsview-core/tests/decode_config.rs`, add tests named `parse_decode_config_with_linear_stack`, `channel_bindings_use_numeric_indexes`, and `option_values_preserve_typed_json_shape` that prove the recommended JSON shape parses and normalizes correctly.</action>
  <acceptance_criteria>
    - `rg -n "pub struct DecodeConfig|pub struct DecodeDecoderConfig|pub struct DecodeStackEntryConfig|pub enum DecodeOptionValue" crates/dsview-core/src/lib.rs` finds the new config-model types.
    - `rg -n 'channels: .*BTreeMap|HashMap' crates/dsview-core/src/lib.rs` shows channel bindings are modeled as canonical-id keyed maps, not arrays of one-key objects.
    - `rg -n "parse_decode_config_with_linear_stack|channel_bindings_use_numeric_indexes|option_values_preserve_typed_json_shape" crates/dsview-core/tests/decode_config.rs` finds the new schema tests.
    - `! rg -n '"stacked decoders"' crates/dsview-core/src/lib.rs` succeeds, proving the machine-facing schema does not directly mirror the GUI persistence label.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p dsview-core --test decode_config -- --nocapture</automated>
  </verify>
  <done>The Rust core now defines the JSON config model Phase 15 needs, with typed option values and a clear linear stack representation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream decoder metadata -> config schema | Option type information must remain accurate enough for later validation and execution |
| user JSON -> core config model | The parser must reject malformed shapes instead of silently coercing them |
| DSView reference JSON -> CLI schema | DSView semantics should influence the model without importing GUI-shaped serialization baggage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-01 | T | option kind metadata | mitigate | Derive a stable value-kind enum from upstream defaults and keep canonical ids untouched |
| T-15-01-02 | I | JSON config parser | mitigate | Parse into typed enums/maps only, with tests that fail on malformed structure or implicit coercion |
| T-15-01-03 | D | future execution path | mitigate | Make the config model directly consumable by later validation/execution instead of introducing a throwaway parsing shape |
</threat_model>

<verification>
- `cargo test -p dsview-sys --test boundary -- --nocapture`
- `cargo test -p dsview-core --test decode_config -- --nocapture`
</verification>

<success_criteria>
- The decode metadata layer exposes explicit option value kinds for validation
- Core owns a JSON-first decode config schema with root decoder, numeric channel bindings, typed options, and a linear stack
- The schema does not invent alias ids or copy DSView's GUI-only persistence structure verbatim
</success_criteria>

<output>
After completion, create `.planning/phases/15-decode-config-model-and-validation/15-01-SUMMARY.md`
</output>
