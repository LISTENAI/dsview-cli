---
phase: 15-decode-config-model-and-validation
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - crates/dsview-core/src/lib.rs
  - crates/dsview-core/tests/decode_config.rs
  - crates/dsview-cli/src/main.rs
autonomous: true
requirements:
  - DEC-04
must_haves:
  truths:
    - "Phase 15 validates decode configs strictly before execution and does not continue with warnings."
    - "Validation distinguishes schema/config errors, metadata/compatibility errors, and runtime prerequisite errors."
    - "Linear stack compatibility is enforced using the same canonical inputs/outputs metadata exposed by Phase 14."
  artifacts:
    - path: crates/dsview-core/src/lib.rs
      provides: "Metadata-driven decode config validator and stable validation error taxonomy"
      contains: "pub enum DecodeConfigValidationError"
    - path: crates/dsview-core/tests/decode_config.rs
      provides: "Validation coverage for schema, metadata, option type, and stack compatibility failures"
      contains: "rejects_incompatible_linear_stack"
    - path: crates/dsview-cli/src/main.rs
      provides: "Decode config validation error mapping aligned with CLI error response conventions"
      contains: "classify_decode_config_validation_error"
  key_links:
    - from: crates/dsview-core/src/lib.rs
      to: crates/dsview-cli/src/main.rs
      via: "core validation errors map cleanly into stable CLI error codes"
      pattern: "DecodeConfigValidationError|classify_decode_config_validation_error"
---

<objective>
Implement strict metadata-driven validation for the new decode config model and define the stable error taxonomy the CLI will expose.

Purpose: After the config schema exists, Phase 15 must prove whether a config is actually valid against decoder metadata, stack compatibility rules, and runtime prerequisites before execution begins.
Output: A strict core validator plus stable validation errors that later CLI commands can surface directly.
</objective>

<execution_context>
@/home/seasonyuu/.codex/get-shit-done/workflows/execute-plan.md
@/home/seasonyuu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-decode-config-model-and-validation/15-CONTEXT.md
@.planning/phases/15-decode-config-model-and-validation/15-RESEARCH.md
@.planning/phases/15-decode-config-model-and-validation/15-VALIDATION.md
@.planning/phases/15-decode-config-model-and-validation/15-01-PLAN.md
@.planning/phases/14-decode-runtime-boundary-and-decoder-registry/14-VERIFICATION.md
@CLAUDE.md
@crates/dsview-core/src/lib.rs
@crates/dsview-cli/src/main.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement strict decode config validation in core</name>
  <files>crates/dsview-core/src/lib.rs, crates/dsview-core/tests/decode_config.rs</files>
  <read_first>
    - .planning/phases/15-decode-config-model-and-validation/15-CONTEXT.md
    - .planning/phases/15-decode-config-model-and-validation/15-RESEARCH.md
    - .planning/phases/15-decode-config-model-and-validation/15-VALIDATION.md
    - crates/dsview-core/src/lib.rs
    - crates/dsview-core/tests/decode_config.rs
    - .planning/phases/14-decode-runtime-boundary-and-decoder-registry/14-VERIFICATION.md
  </read_first>
  <behavior>
    - Validation fails immediately on invalid configs; there is no warning-only path.
    - Validation enforces required channels, known channels/options, typed option values, and linear stack compatibility.
    - Stack compatibility uses canonical `inputs` / `outputs` metadata rather than custom hand-maintained pairing tables.
  </behavior>
  <action>In `crates/dsview-core/src/lib.rs`, add a strict validator that takes the parsed `DecodeConfig` plus decoder discovery metadata and returns either a normalized validated config or a typed `DecodeConfigValidationError`. Include explicit error variants for file/schema problems that survive parsing, unknown decoders, missing required channels, unknown channel bindings, unknown options, invalid option value types/allowed values, and incompatible stack links. Enforce linear stack compatibility by checking that each stacked decoder has at least one input id matching an output id from the previous decoder. In `crates/dsview-core/tests/decode_config.rs`, add tests named `rejects_missing_required_channels`, `rejects_unknown_option_ids`, `rejects_option_value_type_mismatch`, `rejects_incompatible_linear_stack`, and `accepts_valid_linear_stack_against_decoder_metadata`.</action>
  <acceptance_criteria>
    - `rg -n "pub enum DecodeConfigValidationError|validate_decode_config" crates/dsview-core/src/lib.rs` finds the validator and error taxonomy.
    - `rg -n "rejects_missing_required_channels|rejects_unknown_option_ids|rejects_option_value_type_mismatch|rejects_incompatible_linear_stack|accepts_valid_linear_stack_against_decoder_metadata" crates/dsview-core/tests/decode_config.rs` finds the strict validation regressions.
    - `! rg -n "warning" crates/dsview-core/src/lib.rs` succeeds for the new validation path, proving Phase 15 does not implement warning-mode continuation.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p dsview-core --test decode_config -- --nocapture</automated>
  </verify>
  <done>Core can now reject malformed or incompatible decode configs before any execution path begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Map validation failures into stable CLI-oriented diagnostics</name>
  <files>crates/dsview-cli/src/main.rs, crates/dsview-core/src/lib.rs, crates/dsview-core/tests/decode_config.rs</files>
  <read_first>
    - .planning/phases/15-decode-config-model-and-validation/15-CONTEXT.md
    - crates/dsview-cli/src/main.rs
    - crates/dsview-core/src/lib.rs
    - crates/dsview-core/tests/decode_config.rs
  </read_first>
  <behavior>
    - Validation errors map cleanly into stable CLI codes/messages.
    - Runtime prerequisite failures remain distinct from schema and metadata validation failures.
    - The CLI error model stays aligned with the repo's existing decode discovery failure style.
  </behavior>
  <action>In `crates/dsview-cli/src/main.rs`, add a dedicated classifier for decode-config validation failures that maps the new core error variants to stable error codes such as `decode_config_parse_failed`, `decode_config_schema_invalid`, `decode_decoder_not_found`, `decode_missing_required_channel`, `decode_unknown_option`, `decode_option_value_invalid`, and `decode_stack_incompatible`. Keep runtime-precondition failures separate by reusing the existing decode runtime error classification path rather than collapsing them into schema errors. If needed, adjust `crates/dsview-core/src/lib.rs` error variants to preserve enough detail for the CLI mapping. Add a focused unit test in `crates/dsview-core/tests/decode_config.rs` or the CLI file named `validation_error_codes_remain_stable_for_decode_config_failures` that locks the expected code mapping contract.</action>
  <acceptance_criteria>
    - `rg -n "classify_decode_config_validation_error|decode_config_parse_failed|decode_stack_incompatible" crates/dsview-cli/src/main.rs` finds the new CLI-facing error mapper and stable codes.
    - `rg -n "validation_error_codes_remain_stable_for_decode_config_failures" crates/dsview-core/tests/decode_config.rs crates/dsview-cli/src/main.rs` finds the code-mapping regression.
    - `! rg -n "decode_runtime_missing.*decode_config_schema_invalid|decode_decoder_dir_missing.*decode_config_schema_invalid" crates/dsview-cli/src/main.rs` succeeds, proving runtime prerequisite failures remain distinct from schema failures.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p dsview-core --test decode_config -- --nocapture && cargo test -p dsview-cli --bin dsview-cli -- --nocapture</automated>
  </verify>
  <done>The project now has a strict validation engine with stable CLI-friendly failure taxonomy ready for a validation command.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user JSON config -> validator | Invalid shapes or types must fail deterministically before metadata lookup or execution |
| decoder metadata -> stack validator | Stack compatibility must be derived from canonical upstream IO metadata, not guessed |
| core validation -> CLI errors | Machine-readable failure codes must distinguish config problems from environment/runtime problems |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02-01 | T | stack compatibility logic | mitigate | Derive compatibility strictly from canonical inputs/outputs and fail incompatible links |
| T-15-02-02 | I | typed option validation | mitigate | Validate JSON value kinds against explicit decoder option metadata from 15-01 |
| T-15-02-03 | R | CLI error taxonomy | mitigate | Map each validation family to a stable code so users can distinguish bad config from broken environment |
</threat_model>

<verification>
- `cargo test -p dsview-core --test decode_config -- --nocapture`
- `cargo test -p dsview-cli --bin dsview-cli -- --nocapture`
</verification>

<success_criteria>
- Decode configs are rejected strictly when schema, metadata, or stack rules are violated
- Stack compatibility uses Phase 14 inputs/outputs metadata rather than ad-hoc tables
- CLI-facing validation errors are stable and clearly separated from runtime prerequisite errors
</success_criteria>

<output>
After completion, create `.planning/phases/15-decode-config-model-and-validation/15-02-SUMMARY.md`
</output>
