---
phase: 15-decode-config-model-and-validation
plan: 03
type: execute
wave: 3
depends_on:
  - 15-02
files_modified:
  - crates/dsview-cli/src/main.rs
  - crates/dsview-cli/src/lib.rs
  - crates/dsview-cli/tests/decode_cli.rs
  - crates/dsview-core/src/lib.rs
  - crates/dsview-core/tests/decode_config.rs
autonomous: true
requirements:
  - DEC-03
  - DEC-04
must_haves:
  truths:
    - "Users can point the CLI at a JSON decode config file and validate it before execution starts."
    - "Validation success and failure output reuse the project's JSON-first CLI contract and stable error schema."
    - "Phase 15 adds validation only; it does not execute decode sessions or add decoder-specific flags to capture."
  artifacts:
    - path: crates/dsview-cli/src/main.rs
      provides: "`decode validate --config <PATH>` command wiring"
      contains: "Validate(DecodeValidateArgs)"
    - path: crates/dsview-cli/src/lib.rs
      provides: "JSON/text response rendering for decode config validation"
      contains: "render_decode_validate_text"
    - path: crates/dsview-cli/tests/decode_cli.rs
      provides: "CLI contract coverage for valid and invalid decode config validation"
      contains: "decode_validate_accepts_valid_linear_stack_config"
  key_links:
    - from: crates/dsview-cli/src/main.rs
      to: crates/dsview-core/src/lib.rs
      via: "CLI validation command delegates to the strict core decode config validator"
      pattern: "decode validate|validate_decode_config"
---

<objective>
Add the CLI validation command and lock its success/failure contracts for JSON decode configs.

Purpose: Phase 15 should give users a first-class way to validate decode config files before any execution exists. This keeps validation separate from decode execution and proves the config model is usable from the CLI.
Output: `decode validate --config <PATH>` plus JSON/text rendering and CLI regression coverage.
</objective>

<execution_context>
@/home/seasonyuu/.codex/get-shit-done/workflows/execute-plan.md
@/home/seasonyuu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-decode-config-model-and-validation/15-CONTEXT.md
@.planning/phases/15-decode-config-model-and-validation/15-RESEARCH.md
@.planning/phases/15-decode-config-model-and-validation/15-VALIDATION.md
@.planning/phases/15-decode-config-model-and-validation/15-02-PLAN.md
@CLAUDE.md
@crates/dsview-cli/src/main.rs
@crates/dsview-cli/src/lib.rs
@crates/dsview-core/src/lib.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the decode validation CLI command and response models</name>
  <files>crates/dsview-cli/src/main.rs, crates/dsview-cli/src/lib.rs, crates/dsview-core/src/lib.rs</files>
  <read_first>
    - .planning/phases/15-decode-config-model-and-validation/15-CONTEXT.md
    - .planning/phases/15-decode-config-model-and-validation/15-RESEARCH.md
    - crates/dsview-cli/src/main.rs
    - crates/dsview-cli/src/lib.rs
    - crates/dsview-core/src/lib.rs
  </read_first>
  <behavior>
    - The CLI exposes `decode validate --config <PATH>` as the Phase 15 user-facing validation entrypoint.
    - Success output confirms the config is valid and summarizes the root decoder plus stack depth.
    - No decode execution path is triggered in this phase.
  </behavior>
  <action>In `crates/dsview-cli/src/main.rs`, add a `Validate` subcommand under `DecodeCommand` that accepts `--config <PATH>` and the existing shared decode runtime / decoder-dir override flags plus `--format`. Wire it to a new core helper that loads, parses, and validates the JSON config without executing a decode session. In `crates/dsview-cli/src/lib.rs`, add response structs and text rendering helpers for validation success, including fields such as config version, root decoder id, stack length, and bound channel ids. Ensure the command name and help text make it clear this is validation-only, not execution.</action>
  <acceptance_criteria>
    - `rg -n "Validate\(DecodeValidateArgs\)|struct DecodeValidateArgs" crates/dsview-cli/src/main.rs` finds the new CLI command wiring.
    - `rg -n "render_decode_validate_text|DecodeValidateResponse" crates/dsview-cli/src/lib.rs` finds the new response models/renderers.
    - `! rg -n "decode run|DecodeRun" crates/dsview-cli/src/main.rs crates/dsview-cli/src/lib.rs` still succeeds, proving Phase 15 did not pull execution into the command surface.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p dsview-cli --lib -- --nocapture</automated>
  </verify>
  <done>The CLI now has a validation-only command for decode configs with JSON-first responses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock CLI config validation success and failure behavior with tests</name>
  <files>crates/dsview-cli/tests/decode_cli.rs, crates/dsview-cli/src/main.rs, crates/dsview-core/tests/decode_config.rs</files>
  <read_first>
    - .planning/phases/15-decode-config-model-and-validation/15-CONTEXT.md
    - .planning/phases/15-decode-config-model-and-validation/15-RESEARCH.md
    - crates/dsview-cli/tests/decode_cli.rs
    - crates/dsview-cli/src/main.rs
    - crates/dsview-core/tests/decode_config.rs
  </read_first>
  <behavior>
    - Spawned CLI tests cover valid config success and representative invalid-config failures.
    - JSON and text outputs remain stable for automation and shell use.
    - Validation failures return the intended stable error codes instead of generic command errors.
  </behavior>
  <action>In `crates/dsview-cli/tests/decode_cli.rs`, add CLI contract tests named `decode_validate_accepts_valid_linear_stack_config`, `decode_validate_rejects_missing_required_channel`, `decode_validate_rejects_incompatible_stack`, and `decode_validate_reports_schema_errors_with_stable_code`. Use fixture config files or inline temp files to exercise the command with both valid and invalid JSON configs. If needed, add minimal test seams in `crates/dsview-cli/src/main.rs` so tests can run against fixture discovery metadata without requiring a fully packaged runtime in CI, but keep those seams debug-only or explicitly test-scoped. In `crates/dsview-core/tests/decode_config.rs`, add any additional helpers needed so CLI fixtures reflect real validator behavior instead of duplicating validation logic in the test harness.</action>
  <acceptance_criteria>
    - `rg -n "decode_validate_accepts_valid_linear_stack_config|decode_validate_rejects_missing_required_channel|decode_validate_rejects_incompatible_stack|decode_validate_reports_schema_errors_with_stable_code" crates/dsview-cli/tests/decode_cli.rs` finds the CLI contract tests.
    - `cargo test -p dsview-cli --test decode_cli -- --nocapture` exits 0.
    - CLI failure output for invalid configs continues to use stable codes/messages instead of a generic panic or ad-hoc stderr text.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p dsview-cli --test decode_cli -- --nocapture && cargo test -p dsview-core --test decode_config -- --nocapture</automated>
  </verify>
  <done>The Phase 15 validation command is contract-tested for both valid and invalid configs and is ready for future execution integration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-supplied config path -> CLI validator | File/JSON errors must be reported cleanly without being confused with runtime metadata failures |
| core validator -> CLI contract | Validation success/failure must remain stable enough for automation to consume before execution exists |
| phase scope -> command surface | Validation must remain separate from execution so Phase 15 does not overrun into Phase 16 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03-01 | T | validation command surface | mitigate | Expose only `decode validate --config` and explicitly avoid decode execution in Phase 15 |
| T-15-03-02 | I | CLI output contract | mitigate | Lock JSON/text success and error behavior with dedicated spawned CLI tests |
| T-15-03-03 | R | invalid config handling | mitigate | Ensure stable validation codes are returned instead of generic runtime or parser failures |
</threat_model>

<verification>
- `cargo test -p dsview-cli --lib -- --nocapture`
- `cargo test -p dsview-cli --test decode_cli -- --nocapture`
- `cargo test -p dsview-core --test decode_config -- --nocapture`
</verification>

<success_criteria>
- Users can validate a JSON decode config from the CLI before execution exists
- Validation success/failure is exposed through stable JSON/text contracts
- Phase 15 keeps validation separate from execution and does not expand `capture`
</success_criteria>

<output>
After completion, create `.planning/phases/15-decode-config-model-and-validation/15-03-SUMMARY.md`
</output>
