---
phase: 14-decode-runtime-boundary-and-decoder-registry
plan: 03
type: execute
wave: 3
depends_on:
  - 14-02
files_modified:
  - crates/dsview-cli/src/main.rs
  - crates/dsview-cli/src/lib.rs
  - crates/dsview-cli/tests/devices_cli.rs
  - crates/dsview-core/src/lib.rs
  - crates/dsview-core/tests/device_options.rs
autonomous: true
requirements:
  - DEC-01
  - DEC-02
must_haves:
  truths:
    - "Phase 14 CLI scope is limited to `decode list` and `decode inspect <decoder-id>` and does not introduce `decode run`."
    - "JSON is the authoritative contract for decode discovery output; text is a readable rendering of the same canonical metadata."
    - "CLI output preserves canonical upstream decoder, channel, and option ids."
  artifacts:
    - path: crates/dsview-cli/src/main.rs
      provides: "New decode subcommands and argument wiring"
      contains: "DecodeCommand"
    - path: crates/dsview-cli/src/lib.rs
      provides: "JSON/text rendering helpers for decode list and inspect surfaces"
      contains: "render_decode_list_text"
    - path: crates/dsview-cli/tests/devices_cli.rs
      provides: "CLI contract coverage for decode discovery commands"
      contains: "decode_list_json_reports_canonical_decoder_ids"
  key_links:
    - from: crates/dsview-cli/src/main.rs
      to: crates/dsview-core/src/lib.rs
      via: "CLI discovery commands delegate to the new core registry and inspect domain"
      pattern: "decode_list|decode_inspect"
---

<objective>
Add the user-facing Phase 14 CLI commands and lock the output contract for decoder registry and inspect metadata.

Purpose: The previous plans create the runtime and Rust metadata domain; this plan makes the capability visible to users through `decode list` and `decode inspect <decoder-id>` while preserving the project's JSON-first CLI contract.
Output: A shipped CLI discovery surface with stable JSON/text rendering and regression coverage for success and failure paths.
</objective>

<execution_context>
@/home/seasonyuu/.codex/get-shit-done/workflows/execute-plan.md
@/home/seasonyuu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-decode-runtime-boundary-and-decoder-registry/14-CONTEXT.md
@.planning/phases/14-decode-runtime-boundary-and-decoder-registry/14-RESEARCH.md
@.planning/phases/14-decode-runtime-boundary-and-decoder-registry/14-02-PLAN.md
@CLAUDE.md
@crates/dsview-cli/src/main.rs
@crates/dsview-cli/src/lib.rs
@crates/dsview-cli/tests/devices_cli.rs
@crates/dsview-core/src/lib.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add decode discovery command wiring and output models</name>
  <files>crates/dsview-cli/src/main.rs, crates/dsview-cli/src/lib.rs, crates/dsview-core/src/lib.rs</files>
  <read_first>
    - .planning/phases/14-decode-runtime-boundary-and-decoder-registry/14-CONTEXT.md
    - .planning/phases/14-decode-runtime-boundary-and-decoder-registry/14-RESEARCH.md
    - crates/dsview-cli/src/main.rs
    - crates/dsview-cli/src/lib.rs
    - crates/dsview-core/src/lib.rs
  </read_first>
  <behavior>
    - CLI introduces `decode list` and `decode inspect <decoder-id>` only.
    - JSON responses are authoritative and preserve canonical upstream ids.
    - Text rendering remains readable but is derived from the same core response models.
  </behavior>
  <action>In `crates/dsview-cli/src/main.rs`, add a new `decode` command group with `list` and `inspect` subcommands, reusing the existing shared runtime/path argument patterns where they make sense for decode discovery. Do not add `decode run` or any config-file arguments in this phase. In `crates/dsview-cli/src/lib.rs`, add response structs and text-rendering helpers for decoder list and inspect output. In `crates/dsview-core/src/lib.rs`, expose orchestration helpers that call into the new decode discovery domain so CLI command handlers do not talk to sys directly. Ensure inspect output includes channels, options, annotations or rows, and stack-relevant inputs/outputs while preserving upstream ids in JSON.</action>
  <acceptance_criteria>
    - `rg -n "enum DecodeCommand|DecodeListArgs|DecodeInspectArgs" crates/dsview-cli/src/main.rs` finds the new CLI decode subcommands.
    - `rg -n "render_decode_list_text|render_decode_inspect_text|DecodeListResponse|DecodeInspectResponse" crates/dsview-cli/src/lib.rs` finds the new output models and text renderers.
    - `rg -n "decode_list|decode_inspect" crates/dsview-core/src/lib.rs` finds the core orchestration helpers used by the CLI.
    - `! rg -n "decode run|DecodeRun" crates/dsview-cli/src/main.rs crates/dsview-cli/src/lib.rs` succeeds, proving Phase 14 did not overrun its command scope.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p dsview-cli --lib -- --nocapture</automated>
  </verify>
  <done>The CLI command tree and output models now expose decoder discovery and inspect without adding out-of-scope execution commands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock JSON/text command contracts and failure reporting with CLI tests</name>
  <files>crates/dsview-cli/tests/devices_cli.rs, crates/dsview-cli/src/main.rs, crates/dsview-cli/src/lib.rs</files>
  <read_first>
    - .planning/phases/14-decode-runtime-boundary-and-decoder-registry/14-CONTEXT.md
    - .planning/phases/14-decode-runtime-boundary-and-decoder-registry/14-RESEARCH.md
    - crates/dsview-cli/tests/devices_cli.rs
    - crates/dsview-cli/src/main.rs
    - crates/dsview-cli/src/lib.rs
  </read_first>
  <behavior>
    - Spawned CLI tests verify `decode list` and `decode inspect` JSON output, text output, and error paths.
    - Error messages remain specific for missing runtime, missing decoder metadata, and unknown decoder ids.
    - Tests confirm canonical upstream ids are present in the CLI contract rather than a rewritten token namespace.
  </behavior>
  <action>In `crates/dsview-cli/tests/devices_cli.rs`, add spawned CLI coverage for `decode list` and `decode inspect` using the same style the project already uses for command-contract tests. Add tests named `decode_list_json_reports_canonical_decoder_ids`, `decode_inspect_json_includes_channels_options_and_stack_metadata`, `decode_list_text_is_human_readable_but_keeps_canonical_ids_visible`, and `decode_inspect_reports_unknown_decoder_cleanly`. Wire any small test seams needed in `crates/dsview-cli/src/main.rs` or `crates/dsview-cli/src/lib.rs`, but keep them debug-only or fixture-driven if a live decode runtime is not guaranteed in CI.</action>
  <acceptance_criteria>
    - `rg -n "decode_list_json_reports_canonical_decoder_ids|decode_inspect_json_includes_channels_options_and_stack_metadata|decode_list_text_is_human_readable_but_keeps_canonical_ids_visible|decode_inspect_reports_unknown_decoder_cleanly" crates/dsview-cli/tests/devices_cli.rs` finds the new CLI contract tests.
    - `rg -n "unknown decoder|decoder not found|runtime|decoder scripts" crates/dsview-cli/src/main.rs crates/dsview-cli/src/lib.rs` finds specific failure reporting hooks for discovery commands.
    - `rg -n '"id"|canonical' crates/dsview-cli/tests/devices_cli.rs` shows the tests assert upstream ids remain visible in the contract.
    - `cargo test -p dsview-cli --test devices_cli -- --nocapture` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p dsview-cli --test devices_cli -- --nocapture</automated>
  </verify>
  <done>The decode discovery CLI contract is regression-locked for both JSON/text and for the expected failure paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| core registry -> CLI contract | Structured metadata must stay faithful to canonical upstream ids and fields when rendered to users |
| CLI runtime args -> decode discovery | Missing runtime/decoder-path prerequisites must be reported specifically, not as generic command failures |
| phase scope -> command surface | Discovery-only commands must not silently expand into execution/config scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-01 | T | CLI output contract | mitigate | Assert canonical ids in JSON/text tests and avoid token remapping in the renderer |
| T-14-03-02 | D | discovery command failure UX | mitigate | Preserve runtime/decoder/id-specific error messages and test them with spawned CLI coverage |
| T-14-03-03 | E | phase scope discipline | mitigate | Explicitly restrict command additions to `decode list` and `decode inspect` and fail verification if `decode run` appears |
</threat_model>

<verification>
- `cargo test -p dsview-cli --lib -- --nocapture`
- `cargo test -p dsview-cli --test devices_cli -- --nocapture`
</verification>

<success_criteria>
- Users can list available decoders and inspect a specific decoder from the CLI
- JSON output preserves canonical upstream ids and includes the metadata promised by Phase 14
- Text output remains readable while reflecting the same canonical data
- CLI failure paths are specific and regression-tested
</success_criteria>

<output>
After completion, create `.planning/phases/14-decode-runtime-boundary-and-decoder-registry/14-03-SUMMARY.md`
</output>
