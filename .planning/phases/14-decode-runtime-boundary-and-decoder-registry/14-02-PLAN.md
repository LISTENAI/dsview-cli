---
phase: 14-decode-runtime-boundary-and-decoder-registry
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - crates/dsview-sys/src/lib.rs
  - crates/dsview-core/src/lib.rs
  - crates/dsview-core/tests/device_options.rs
  - crates/dsview-sys/tests/boundary.rs
autonomous: true
requirements:
  - DEC-01
  - DEC-02
must_haves:
  truths:
    - "Safe Rust code works with structured decoder registry and inspect models, not raw bridge pointers."
    - "Canonical upstream decoder/channel/option ids remain preserved in the Rust domain model."
    - "The registry/inspect domain is rich enough for later config validation work without adding a second canonical token namespace."
  artifacts:
    - path: crates/dsview-sys/src/lib.rs
      provides: "Safe decode discovery wrappers over the raw bridge surface"
      contains: "pub fn decode_list"
    - path: crates/dsview-core/src/lib.rs
      provides: "Rust-owned decoder registry and inspect domain types"
      contains: "pub struct DecoderDescriptor"
    - path: crates/dsview-core/tests/device_options.rs
      provides: "Pure Rust coverage for decoder metadata normalization and id preservation"
      contains: "preserves_upstream_decoder_ids"
  key_links:
    - from: crates/dsview-sys/src/lib.rs
      to: crates/dsview-core/src/lib.rs
      via: "owned sys snapshots are lifted into typed core models without semantic id remapping"
      pattern: "DecoderDescriptor|DecoderChannelDescriptor|DecoderOptionDescriptor"
---

<objective>
Build the safe Rust registry and inspect domain on top of the raw decode bridge, preserving upstream ids while exposing a typed model that later config and CLI layers can consume.

Purpose: Phase 14 should not leave decoder metadata trapped in raw FFI structs. This plan creates the safe `dsview-sys` wrappers and the `dsview-core` domain model that define what a decoder looks like inside the Rust application.
Output: Safe list/inspect APIs and core descriptor types for decoders, channels, options, annotations, and stack-relevant inputs/outputs.
</objective>

<execution_context>
@/home/seasonyuu/.codex/get-shit-done/workflows/execute-plan.md
@/home/seasonyuu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-decode-runtime-boundary-and-decoder-registry/14-CONTEXT.md
@.planning/phases/14-decode-runtime-boundary-and-decoder-registry/14-RESEARCH.md
@.planning/phases/14-decode-runtime-boundary-and-decoder-registry/14-01-PLAN.md
@CLAUDE.md
@crates/dsview-sys/src/lib.rs
@crates/dsview-core/src/lib.rs
@crates/dsview-core/tests/device_options.rs
@crates/dsview-sys/tests/boundary.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wrap raw decode discovery snapshots in safe sys-layer APIs</name>
  <files>crates/dsview-sys/src/lib.rs, crates/dsview-sys/tests/boundary.rs</files>
  <read_first>
    - .planning/phases/14-decode-runtime-boundary-and-decoder-registry/14-CONTEXT.md
    - .planning/phases/14-decode-runtime-boundary-and-decoder-registry/14-RESEARCH.md
    - .planning/phases/14-decode-runtime-boundary-and-decoder-registry/14-01-PLAN.md
    - crates/dsview-sys/src/lib.rs
    - crates/dsview-sys/tests/boundary.rs
  </read_first>
  <behavior>
    - The sys crate exposes safe `decode_list` and `decode_inspect` entrypoints that return owned Rust data structures.
    - Wrapper code preserves canonical upstream ids exactly as returned by the bridge.
    - Error conversion keeps the distinct loader/init/not-found/upstream-failure categories intact.
  </behavior>
  <action>In `crates/dsview-sys/src/lib.rs`, add safe wrapper structs and methods for decode discovery, such as decoder, channel, option, annotation-row, input, and output descriptors, all built from the raw bridge snapshots added in Plan 14-01. Ensure every wrapper performs owned conversion and frees the raw bridge allocations deterministically. Add public methods equivalent to `decode_list()` and `decode_inspect(decoder_id)` on the sys runtime boundary, and preserve upstream ids exactly in the returned Rust values. In `crates/dsview-sys/tests/boundary.rs`, add tests that verify the safe wrappers reject unknown decoder ids cleanly and preserve raw ids/labels when converting from synthetic bridge data or fixtures.</action>
  <acceptance_criteria>
    - `rg -n "pub fn decode_list|pub fn decode_inspect|struct DecodeDecoder|struct DecodeChannel|struct DecodeOption" crates/dsview-sys/src/lib.rs` finds safe list/inspect APIs and owned wrapper types.
    - `rg -n "unknown decoder|not found|loader|init" crates/dsview-sys/src/lib.rs` finds differentiated decode discovery error conversion.
    - `rg -n "preserve|upstream id|decode_inspect" crates/dsview-sys/tests/boundary.rs` finds coverage for id preservation and inspect failures.
    - `! rg -n "stable_id|token" crates/dsview-sys/src/lib.rs` succeeds, proving Phase 14 safe wrappers do not invent a new canonical id layer.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p dsview-sys --test boundary -- --nocapture</automated>
  </verify>
  <done>The sys crate now returns fully owned safe decoder metadata and preserves explicit discovery error categories.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Introduce core decoder registry and inspect domain types</name>
  <files>crates/dsview-core/src/lib.rs, crates/dsview-core/tests/device_options.rs</files>
  <read_first>
    - .planning/phases/14-decode-runtime-boundary-and-decoder-registry/14-CONTEXT.md
    - .planning/phases/14-decode-runtime-boundary-and-decoder-registry/14-RESEARCH.md
    - crates/dsview-core/src/lib.rs
    - crates/dsview-core/tests/device_options.rs
    - crates/dsview-sys/src/lib.rs
  </read_first>
  <behavior>
    - Core exposes typed registry/inspect models for CLI consumption and later config validation.
    - Required and optional channels stay distinct.
    - Inputs, outputs, options, and annotation metadata are modeled explicitly enough for later planner phases to reuse without reparsing raw sys data.
  </behavior>
  <action>In `crates/dsview-core/src/lib.rs`, add a decode discovery domain model with exported types such as `DecoderDescriptor`, `DecoderChannelDescriptor`, `DecoderOptionDescriptor`, `DecoderAnnotationRowDescriptor`, and any helper functions needed to map safe sys-layer metadata into the core model. Preserve canonical upstream ids exactly in the core model while normalizing structural shape for downstream CLI use. Keep required and optional channels separate, and expose stack-relevant inputs/outputs in the inspect model so Phase 15 can validate stack compatibility without adding another metadata query path. In `crates/dsview-core/tests/device_options.rs`, add focused tests that assert upstream ids are preserved, required/optional channels remain distinct, and decoder inspect metadata remains complete enough for future config validation.</action>
  <acceptance_criteria>
    - `rg -n "pub struct DecoderDescriptor|pub struct DecoderChannelDescriptor|pub struct DecoderOptionDescriptor|pub struct DecoderAnnotationRowDescriptor" crates/dsview-core/src/lib.rs` finds the new core decoder domain types.
    - `rg -n "required_channels|optional_channels|inputs|outputs" crates/dsview-core/src/lib.rs` finds explicit structural fields needed by later phases.
    - `rg -n "preserves_upstream_decoder_ids|keeps_required_and_optional_channels_distinct|exposes_stack_relevant_inputs_and_outputs" crates/dsview-core/tests/device_options.rs` finds the new core coverage.
    - `! rg -n "stable_id|token" crates/dsview-core/src/lib.rs` succeeds, proving the core model also keeps upstream ids canonical in Phase 14.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p dsview-core --test device_options -- --nocapture && cargo test -p dsview-core --lib -- --nocapture</automated>
  </verify>
  <done>The core crate now owns a stable structured decoder registry/inspect model that later phases can build on directly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw bridge snapshots -> safe sys wrappers | Memory ownership and nested arrays must be converted safely and completely |
| sys wrappers -> core domain | Structural normalization must not mutate canonical upstream ids or collapse required/optional distinctions |
| core domain -> future config/CLI work | Missing metadata fields now would force later phases to add parallel metadata paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-01 | I | sys safe wrapper conversion | mitigate | Deterministic owned conversion plus tests for free/error paths |
| T-14-02-02 | T | core metadata normalization | mitigate | Preserve upstream ids verbatim and ban token/stable-id remapping in Phase 14 |
| T-14-02-03 | D | future stack/config work | mitigate | Include explicit inputs/outputs and required/optional channel structure in the core inspect model now |
</threat_model>

<verification>
- `cargo test -p dsview-sys --test boundary -- --nocapture`
- `cargo test -p dsview-core --test device_options -- --nocapture`
- `cargo test -p dsview-core --lib -- --nocapture`
</verification>

<success_criteria>
- Safe sys wrappers expose decode list/inspect metadata without raw-pointer leakage
- Core owns a structured decoder registry/inspect model with canonical upstream ids intact
- Required/optional channels and stack-relevant inputs/outputs are available for later phases
- Error categories remain distinct enough for CLI reporting work in Plan 14-03
</success_criteria>

<output>
After completion, create `.planning/phases/14-decode-runtime-boundary-and-decoder-registry/14-02-SUMMARY.md`
</output>
