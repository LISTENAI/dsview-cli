---
phase: 10-device-option-bridge-and-discovery
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - crates/dsview-core/src/lib.rs
  - crates/dsview-core/src/device_options.rs
  - crates/dsview-core/tests/device_options.rs
autonomous: true
requirements:
  - OPT-01
must_haves:
  truths:
    - Rust core returns a stable automation-facing device-option snapshot for a selected DSLogic Plus device
    - Stable IDs are derived from explicit metadata or raw codes, not from human labels
    - Channel modes stay nested under each operation mode and threshold remains a truthful capability snapshot
  artifacts:
    - path: crates/dsview-core/src/device_options.rs
      provides: Normalized Phase 10 device-option model
      contains: pub struct DeviceOptionsSnapshot
    - path: crates/dsview-core/src/lib.rs
      provides: Discovery entrypoint that opens a selected device and returns normalized option data
      contains: pub fn inspect_device_options
    - path: crates/dsview-core/tests/device_options.rs
      provides: Stable-ID, ordering, and threshold-shape coverage
      contains: stable
  key_links:
    - from: crates/dsview-core/src/lib.rs
      to: crates/dsview-sys/src/lib.rs
      via: RuntimeBridge device-option snapshot
      pattern: device_options
    - from: crates/dsview-core/src/device_options.rs
      to: crates/dsview-cli/src/main.rs
      via: serde-serializable normalized snapshot
      pattern: Serialize
---

<objective>
Normalize the native DSLogic Plus option snapshot into a stable Rust model that later CLI automation can consume directly.

Purpose: Separate Phase 10 discovery semantics from the existing capture-validation model so later phases can evolve configuration rules without rewriting the discovery contract.
Output: A new core device-option model, public discovery API, and regression tests for stable IDs, ordering, and threshold shape.
</objective>

<execution_context>
@/home/seasonyuu/.codex/get-shit-done/workflows/execute-plan.md
@/home/seasonyuu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-device-option-bridge-and-discovery/10-RESEARCH.md
@.planning/phases/10-device-option-bridge-and-discovery/10-VALIDATION.md
@CLAUDE.md
@crates/dsview-core/src/lib.rs
@crates/dsview-core/src/capture_config.rs
@crates/dsview-sys/src/lib.rs

<interfaces>
From `crates/dsview-core/src/lib.rs` today:

```rust
pub struct Discovery {
    runtime: RuntimeBridge,
    resources: ResourceDirectory,
}

impl Discovery {
    pub fn list_supported_devices(&self) -> Result<Vec<SupportedDevice>, BringUpError>;
    pub fn open_device(
        &self,
        selection_handle: SelectionHandle,
    ) -> Result<OpenedDevice<'_>, BringUpError>;
}
```

From `crates/dsview-core/src/capture_config.rs` today:

```rust
pub struct CaptureCapabilities {
    pub total_channel_count: u16,
    pub active_channel_mode: i16,
    pub channel_modes: Vec<ChannelModeCapability>,
    pub hardware_sample_capacity: u64,
    pub sample_limit_alignment: u64,
    pub threshold_volts: Option<f64>,
}
```

Plan 10-02 should add a dedicated discovery model instead of extending `CaptureCapabilities` with Phase 10-only semantics.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the normalized core model and selected-device discovery API</name>
  <files>crates/dsview-core/src/lib.rs, crates/dsview-core/src/device_options.rs, crates/dsview-core/tests/device_options.rs</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/10-device-option-bridge-and-discovery/10-RESEARCH.md
    - crates/dsview-core/src/lib.rs
    - crates/dsview-core/src/capture_config.rs
    - crates/dsview-sys/src/lib.rs
  </read_first>
  <behavior>
    - `normalizes_option_ids_without_label_parsing` proves enum IDs are `operation-mode:<code>`, `stop-option:<code>`, `filter:<code>`, and `channel-mode:<code>` while labels remain display-only.
    - `selected_device_snapshot_preserves_channel_mode_groups` proves channel modes stay nested under each operation mode with explicit current IDs.
  </behavior>
  <action>Create `crates/dsview-core/src/device_options.rs` and export it from `crates/dsview-core/src/lib.rs`. Add a public `DeviceOptionsSnapshot` model that contains device identity, current values, supported enum options, `channel_modes_by_operation_mode`, and a `ThresholdCapabilitySnapshot`. Add `Discovery::inspect_device_options(selection_handle)` that opens the selected device, consumes the sys snapshot from Plan 10-01, and returns the normalized model. Preserve raw numeric codes alongside stable IDs and labels. Do not reuse `CaptureCapabilities` for this feature; keep discovery and validation models separate so Phase 11 can add validation rules without changing the Phase 10 discovery contract.</action>
  <acceptance_criteria>
    - `rg -n "pub struct DeviceOptionsSnapshot|pub fn inspect_device_options" crates/dsview-core/src/device_options.rs crates/dsview-core/src/lib.rs` finds the new normalized model and discovery API.
    - `rg -n "operation-mode:<code>|stop-option:<code>|filter:<code>|channel-mode:<code>" .planning/phases/10-device-option-bridge-and-discovery/10-02-PLAN.md crates/dsview-core/tests/device_options.rs` confirms tests lock the code-backed ID contract.
    - `cargo test -p dsview-core --test device_options -- --nocapture` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p dsview-core --test device_options -- --nocapture</automated>
  </verify>
  <done>`dsview-core` exposes a stable selected-device option snapshot that is ready for CLI rendering and preserves raw DSView facts.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize threshold, ordering, and compatibility boundaries in the core snapshot</name>
  <files>crates/dsview-core/src/lib.rs, crates/dsview-core/src/device_options.rs, crates/dsview-core/tests/device_options.rs</files>
  <read_first>
    - .planning/phases/10-device-option-bridge-and-discovery/10-RESEARCH.md
    - .planning/phases/10-device-option-bridge-and-discovery/10-VALIDATION.md
    - crates/dsview-core/src/device_options.rs
    - crates/dsview-core/src/lib.rs
    - crates/dsview-core/tests/export_artifacts.rs
  </read_first>
  <action>Lock the public semantics before the CLI consumes them. Represent threshold truthfully as a capability snapshot rooted in `threshold:vth-range` with `kind = "voltage-range"`, `current_volts`, `min_volts`, `max_volts`, and `step_volts`; if legacy `SR_CONF_THRESHOLD` metadata is available from Plan 10-01, expose it only as supplementary raw metadata. Preserve all driver-exposed operation modes in discovery output, including modes that later phases may choose not to set, and keep deterministic ordering by sorting enum groups by raw native code and channel-mode groups by `(operation_mode_code, channel_mode_code)`. Re-run existing core library tests to prove the Phase 9 capture/export baseline still compiles and behaves unchanged.</action>
  <acceptance_criteria>
    - `rg -n "threshold:vth-range|channel_modes_by_operation_mode|native_code" crates/dsview-core/src/device_options.rs crates/dsview-core/tests/device_options.rs` finds the threshold shape, grouped channel modes, and raw-code preservation.
    - `cargo test -p dsview-core --test device_options -- --nocapture && cargo test -p dsview-core --lib -- --nocapture` exits 0.
    - `rg -n "pub use capture_config|validate_capture_config|run_capture" crates/dsview-core/src/lib.rs` still finds the shipped capture/export entrypoints.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p dsview-core --test device_options -- --nocapture && cargo test -p dsview-core --lib -- --nocapture</automated>
  </verify>
  <done>The core snapshot has stable ordering, a truthful threshold contract, and no regression to the existing capture/export API surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `dsview-core` → `dsview-sys` | Safe Rust consumes raw-but-owned native facts and must preserve meaning while normalizing them |
| Core discovery API → CLI/renderers | Stable IDs and ordering become automation contracts once surfaced by the CLI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | S/T | Stable-ID normalization in `crates/dsview-core/src/device_options.rs` | mitigate | Generate machine IDs from explicit numeric codes, keep labels separate, and preserve raw codes in the public model |
| T-10-02-02 | D | Optional option groups in `inspect_device_options` | mitigate | Treat unavailable native groups as optional fields instead of aborting the whole discovery snapshot |
| T-10-02-03 | T/R | Threshold capability mapping | mitigate | Lock the Phase 10 threshold schema in tests and document it as discovery-only so later setter semantics cannot silently drift |
</threat_model>

<verification>
- `cargo test -p dsview-core --test device_options -- --nocapture`
- `cargo test -p dsview-core --lib -- --nocapture`
</verification>

<success_criteria>
- `dsview-core` can return a normalized DSLogic Plus option snapshot for a selected device
- Stable IDs are code-backed and deterministic
- Threshold and channel-mode grouping are modeled truthfully for CLI consumption
- Existing capture/export APIs stay intact for the shipped `v1.0` baseline
</success_criteria>

<output>
After completion, create `.planning/phases/10-device-option-bridge-and-discovery/10-02-SUMMARY.md`
</output>
