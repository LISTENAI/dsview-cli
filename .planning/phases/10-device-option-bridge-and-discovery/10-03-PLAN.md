---
phase: 10-device-option-bridge-and-discovery
plan: 03
type: execute
wave: 3
depends_on:
  - 10-02
files_modified:
  - crates/dsview-cli/src/lib.rs
  - crates/dsview-cli/src/device_options.rs
  - crates/dsview-cli/src/main.rs
  - crates/dsview-cli/tests/device_options_cli.rs
autonomous: true
requirements:
  - OPT-01
must_haves:
  truths:
    - A user can inspect the DSLogic Plus option surface for a selected device from the CLI
    - JSON output exposes stable IDs, labels, raw codes, and threshold capability in a deterministic schema
    - Text output stays stable and readable without disturbing the shipped capture/export commands
  artifacts:
    - path: crates/dsview-cli/src/device_options.rs
      provides: Pure response and rendering helpers for device-option discovery output
      contains: fn render_device_options_text
    - path: crates/dsview-cli/src/main.rs
      provides: `devices options` command wiring
      contains: DeviceCommand::Options
    - path: crates/dsview-cli/tests/device_options_cli.rs
      provides: CLI regression coverage for help, errors, and text/JSON output shape
      contains: devices_options
  key_links:
    - from: crates/dsview-cli/src/main.rs
      to: crates/dsview-core/src/lib.rs
      via: Discovery::inspect_device_options
      pattern: inspect_device_options
    - from: crates/dsview-cli/src/device_options.rs
      to: crates/dsview-cli/tests/device_options_cli.rs
      via: shared rendering helpers and golden assertions
      pattern: render_device_options_text
---

<objective>
Expose the Phase 10 device-option discovery surface in the CLI with stable text and JSON output.

Purpose: Make the DSView-backed DSLogic Plus option surface inspectable from scripts and humans without changing the shipped `v1.0` capture/export path.
Output: A `devices options` CLI command, deterministic renderers, and automated CLI coverage.
</objective>

<execution_context>
@/home/seasonyuu/.codex/get-shit-done/workflows/execute-plan.md
@/home/seasonyuu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-device-option-bridge-and-discovery/10-RESEARCH.md
@.planning/phases/10-device-option-bridge-and-discovery/10-VALIDATION.md
@CLAUDE.md
@crates/dsview-cli/src/main.rs
@crates/dsview-cli/tests/devices_cli.rs
@crates/dsview-cli/tests/capture_cli.rs
@crates/dsview-core/src/lib.rs

<interfaces>
From `crates/dsview-cli/src/main.rs` today:

```rust
enum DeviceCommand {
    List(ListArgs),
    Open(OpenArgs),
}

#[derive(Clone, Copy, Debug, Eq, PartialEq, ValueEnum)]
enum OutputFormat {
    Json,
    Text,
}
```

Expected from Plan 10-02:

```rust
pub struct DeviceOptionsSnapshot { /* stable IDs, labels, raw codes, threshold capability */ }

impl Discovery {
    pub fn inspect_device_options(
        &self,
        selection_handle: SelectionHandle,
    ) -> Result<DeviceOptionsSnapshot, BringUpError>;
}
```

Plan 10-03 should keep JSON authoritative and text deterministic, matching the existing CLI contract style.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pure renderers and CLI tests for stable option-discovery output</name>
  <files>crates/dsview-cli/src/lib.rs, crates/dsview-cli/src/device_options.rs, crates/dsview-cli/tests/device_options_cli.rs</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/10-device-option-bridge-and-discovery/10-RESEARCH.md
    - crates/dsview-cli/src/main.rs
    - crates/dsview-cli/tests/devices_cli.rs
    - crates/dsview-core/src/lib.rs
  </read_first>
  <behavior>
    - `device_options_json_is_stable_for_automation` proves the JSON shape contains current values, supported value lists, raw native codes, and threshold capability fields.
    - `device_options_text_uses_deterministic_section_order` proves text output order is `device`, `operation_modes`, `stop_options`, `filters`, `threshold`, `channel_modes_by_operation_mode`.
  </behavior>
  <action>Create `crates/dsview-cli/src/lib.rs` and `crates/dsview-cli/src/device_options.rs` so the new response-building and rendering logic can be tested without live hardware. Add `crates/dsview-cli/tests/device_options_cli.rs` with golden-style assertions for text and JSON rendering of the response helpers only; do not require the `devices options` command wiring or help surface in this task. The JSON payload must include `selected_handle`, `device`, `operation_modes`, `stop_options`, `filters`, `threshold`, and `channel_modes_by_operation_mode`; each enum option must expose `id`, `label`, and `native_code`. The text renderer must use deterministic section order and stable line prefixes, not ad-hoc pretty-printing.</action>
  <acceptance_criteria>
    - `rg -n "render_device_options_text|DeviceOptionsResponse" crates/dsview-cli/src/lib.rs crates/dsview-cli/src/device_options.rs` finds the new pure rendering surface.
    - `rg -n "operation_modes|channel_modes_by_operation_mode|native_code" crates/dsview-cli/tests/device_options_cli.rs` shows the JSON schema is test-locked.
    - `cargo test -p dsview-cli --test device_options_cli -- --nocapture` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p dsview-cli --test device_options_cli -- --nocapture</automated>
  </verify>
  <done>The CLI crate has testable render helpers and automated coverage for stable device-option output semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Wire `devices options` into the CLI without regressing `v1.0` commands</name>
  <files>crates/dsview-cli/src/main.rs, crates/dsview-cli/src/lib.rs, crates/dsview-cli/src/device_options.rs, crates/dsview-cli/tests/device_options_cli.rs</files>
  <read_first>
    - .planning/phases/10-device-option-bridge-and-discovery/10-VALIDATION.md
    - crates/dsview-cli/src/main.rs
    - crates/dsview-cli/src/device_options.rs
    - crates/dsview-cli/tests/device_options_cli.rs
    - crates/dsview-cli/tests/capture_cli.rs
  </read_first>
  <action>Add `DeviceCommand::Options` and a matching `OptionsArgs` block that reuses `SharedRuntimeArgs` plus `--handle`. Call `Discovery::inspect_device_options()` from Plan 10-02, render JSON through the new response type, and render text through the pure formatter from Task 1. Reuse existing invalid-handle and runtime error classification so the new command keeps stable error vocabulary. Keep `devices list`, `devices open`, and `capture` behavior unchanged. After wiring, expand `device_options_cli.rs` to cover spawned-process help output and the non-hardware error path for invalid handles, then rerun existing CLI integration suites to guard the shipped baseline.</action>
  <acceptance_criteria>
    - `rg -n "DeviceCommand::Options|devices options" crates/dsview-cli/src/main.rs crates/dsview-cli/tests/device_options_cli.rs` finds the new subcommand and help coverage.
    - `cargo test -p dsview-cli --test device_options_cli -- --nocapture && cargo test -p dsview-cli --test devices_cli -- --nocapture && cargo test -p dsview-cli --test capture_cli -- --nocapture` exits 0.
    - `rg -n "invalid_selector|json is stable for automation|text is for direct shell use" crates/dsview-cli/src/main.rs crates/dsview-cli/tests/device_options_cli.rs` confirms stable UX/error wording remains explicit.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p dsview-cli --test device_options_cli -- --nocapture && cargo test -p dsview-cli --test devices_cli -- --nocapture && cargo test -p dsview-cli --test capture_cli -- --nocapture</automated>
  </verify>
  <done>The CLI exposes `devices options --handle <HANDLE>` with stable text/JSON output and the existing `v1.0` command suite remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → core discovery API | User-provided selectors and format choices must not produce unstable or ambiguous output contracts |
| Core snapshot → CLI output | Once rendered, field names, ordering, and error codes become automation-facing contracts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | T/R | CLI output schema in `crates/dsview-cli/src/device_options.rs` | mitigate | Lock JSON field names and text section ordering in `crates/dsview-cli/tests/device_options_cli.rs` |
| T-10-03-02 | D | Selector handling in `crates/dsview-cli/src/main.rs` | mitigate | Reuse `SelectionHandle` validation and existing `invalid_selector` error path for zero/invalid handles |
| T-10-03-03 | T | Shared command surface in `crates/dsview-cli/src/main.rs` | mitigate | Keep new wiring isolated to `devices options` and rerun `devices_cli` + `capture_cli` integration suites before completion |
</threat_model>

<verification>
- `cargo test -p dsview-cli --test device_options_cli -- --nocapture`
- `cargo test -p dsview-cli --test devices_cli -- --nocapture`
- `cargo test -p dsview-cli --test capture_cli -- --nocapture`
</verification>

<success_criteria>
- The CLI exposes a stable `devices options` discovery command for selected DSLogic Plus devices
- JSON is authoritative and deterministic for automation
- Text output is stable and readable with fixed section ordering
- Existing `devices` and `capture` commands stay green
</success_criteria>

<output>
After completion, create `.planning/phases/10-device-option-bridge-and-discovery/10-03-SUMMARY.md`
</output>
