---
phase: 08-verification-backfill-for-acquisition-and-export
plan: 02
title: Backfill durable Phase 5 export verification for audit closure
wave: 2
autonomous: true
depends_on: [08-01]
requirements: [EXP-01, EXP-02, EXP-03, EXP-04]
files_modified:
  - .planning/phases/08-verification-backfill-for-acquisition-and-export/08-02-PLAN.md
  - .planning/phases/05-export-artifacts/05-VERIFICATION.md
  - .planning/REQUIREMENTS.md
must_haves:
  truths:
    - `.planning/phases/05-export-artifacts/05-VERIFICATION.md` does not exist yet and is the main remaining blocker for formal closure of `EXP-01`, `EXP-02`, `EXP-03`, and `EXP-04`.
    - `.planning/phases/05-export-artifacts/05-VALIDATION.md` and `.planning/phases/05-export-artifacts/05-UAT.md` are evidence inputs only; they do not by themselves close `EXP-*` without a requirement-level verification artifact.
    - `EXP-02` closure must preserve the existing Nyquist-safe and timing-plausibility caveats rather than flattening them into broader claims than the documented evidence supports.
    - `EXP-04` closure must be grounded in observed metadata facts recorded by the existing export path and manual evidence, not just requested configuration values.
    - Phase 8 must not hand-edit `.planning/v1.0-MILESTONE-AUDIT.md`; after `05-VERIFICATION.md` and requirements reconciliation exist, the plan must hand off to a fresh `/gsd:audit-milestone` rerun.
    - Phase 8 scope remains documentation and evidence closure only: no product-code edits, no DSView submodule changes, and no unrelated Phase 1 or Phase 6 cleanup.
  artifacts:
    - .planning/phases/05-export-artifacts/05-VERIFICATION.md
    - .planning/phases/05-export-artifacts/05-VALIDATION.md
    - .planning/phases/05-export-artifacts/05-UAT.md
    - .planning/phases/05-export-artifacts/05-01-SUMMARY.md
    - .planning/phases/05-export-artifacts/05-02-SUMMARY.md
    - .planning/phases/05-export-artifacts/05-03-SUMMARY.md
    - .planning/REQUIREMENTS.md
  key_links:
    - from: .planning/phases/05-export-artifacts/05-VERIFICATION.md
      to: .planning/phases/05-export-artifacts/05-VALIDATION.md
      via: automated validation matrix and manual-only verification rows that provide the strongest export evidence inputs
    - from: .planning/phases/05-export-artifacts/05-VERIFICATION.md
      to: .planning/phases/05-export-artifacts/05-UAT.md
      via: real-hardware artifact creation, timing plausibility, metadata plausibility, and immediate post-export reuse evidence
    - from: .planning/phases/05-export-artifacts/05-VERIFICATION.md
      to: .planning/phases/05-export-artifacts/05-01-SUMMARY.md
      via: upstream-backed VCD export path and clean-success-gated export semantics for `EXP-01` and `EXP-02`
    - from: .planning/phases/05-export-artifacts/05-VERIFICATION.md
      to: .planning/phases/05-export-artifacts/05-02-SUMMARY.md
      via: metadata sidecar schema and CLI artifact-reporting semantics for `EXP-03` and `EXP-04`
    - from: .planning/phases/05-export-artifacts/05-VERIFICATION.md
      to: .planning/phases/05-export-artifacts/05-03-SUMMARY.md
      via: final export validation closeout and successful post-fix hardware rerun evidence
    - from: .planning/REQUIREMENTS.md
      to: .planning/phases/05-export-artifacts/05-VERIFICATION.md
      via: replace stale Phase 8 pending export rows with durable requirement-level closure once the verification artifact exists
---

<objective>
Create the missing durable Phase 5 verification artifact so `EXP-01`, `EXP-02`, `EXP-03`, and `EXP-04` can be closed from persistent requirement-level evidence instead of validation and UAT alone.
</objective>

<scope>
This plan only backfills export verification and final requirement traceability for already-shipped behavior. It does not rerun product implementation work, does not edit `DSView/`, does not broaden into Phase 6 workflow cleanup, and does not modify the milestone audit file directly.
</scope>

<task type="auto">
  <name>Build the Phase 5 requirement-evidence map before authoring the verification artifact</name>
  <read_first>
    - .planning/phases/08-verification-backfill-for-acquisition-and-export/08-02-PLAN.md
    - .planning/v1.0-MILESTONE-AUDIT.md
    - .planning/phases/05-export-artifacts/05-VALIDATION.md
    - .planning/phases/05-export-artifacts/05-UAT.md
    - .planning/phases/05-export-artifacts/05-01-SUMMARY.md
    - .planning/phases/05-export-artifacts/05-02-SUMMARY.md
    - .planning/phases/05-export-artifacts/05-03-SUMMARY.md
    - .planning/phases/05-export-artifacts/05-RESEARCH.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <files>.planning/phases/05-export-artifacts/05-VERIFICATION.md, .planning/REQUIREMENTS.md</files>
  <action>Build an explicit requirement-by-requirement evidence map for `EXP-01`, `EXP-02`, `EXP-03`, and `EXP-04` using the Phase 5 validation matrix, manual UAT results, Phase 5 summaries, and the export research guardrails. For `EXP-01`, map successful VCD creation and post-export reuse evidence. For `EXP-02`, map both automated golden/timing evidence and manual Nyquist-safe plausibility checks. For `EXP-03`, map metadata sidecar creation and CLI artifact-reporting evidence. For `EXP-04`, map the observed metadata fields required by the requirement and explicitly distinguish observed facts from requested settings. Use this map as the authoring contract for `05-VERIFICATION.md`.</action>
  <acceptance_criteria>
    - `grep -n "^requirements: \[EXP-01, EXP-02, EXP-03, EXP-04\]$" /home/seasonyuu/projects/dsview-cli/.planning/phases/08-verification-backfill-for-acquisition-and-export/08-02-PLAN.md` returns a match
    - `grep -n "inputs only; they do not by themselves close `EXP-\*`" /home/seasonyuu/projects/dsview-cli/.planning/phases/08-verification-backfill-for-acquisition-and-export/08-02-PLAN.md` returns a match
    - `grep -n "Nyquist-safe" /home/seasonyuu/projects/dsview-cli/.planning/phases/08-verification-backfill-for-acquisition-and-export/08-02-PLAN.md` returns a match
    - `grep -n "observed metadata facts" /home/seasonyuu/projects/dsview-cli/.planning/phases/08-verification-backfill-for-acquisition-and-export/08-02-PLAN.md` returns a match
    - `grep -n "must not hand-edit `.planning/v1.0-MILESTONE-AUDIT.md`" /home/seasonyuu/projects/dsview-cli/.planning/phases/08-verification-backfill-for-acquisition-and-export/08-02-PLAN.md` returns a match
    - `grep -n "fresh `/gsd:audit-milestone` rerun" /home/seasonyuu/projects/dsview-cli/.planning/phases/08-verification-backfill-for-acquisition-and-export/08-02-PLAN.md` returns a match
    - `grep -n "EXP-01" /home/seasonyuu/projects/dsview-cli/.planning/phases/05-export-artifacts/05-VALIDATION.md` returns a match
    - `grep -n "EXP-02" /home/seasonyuu/projects/dsview-cli/.planning/phases/05-export-artifacts/05-VALIDATION.md` returns a match
    - `grep -n "EXP-03" /home/seasonyuu/projects/dsview-cli/.planning/phases/05-export-artifacts/05-VALIDATION.md` returns a match
    - `grep -n "EXP-04" /home/seasonyuu/projects/dsview-cli/.planning/phases/05-export-artifacts/05-VALIDATION.md` returns a match
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Create the durable Phase 5 verification artifact in the established verification style</name>
  <read_first>
    - .planning/phases/05-export-artifacts/05-VALIDATION.md
    - .planning/phases/05-export-artifacts/05-UAT.md
    - .planning/phases/05-export-artifacts/05-01-SUMMARY.md
    - .planning/phases/05-export-artifacts/05-02-SUMMARY.md
    - .planning/phases/05-export-artifacts/05-03-SUMMARY.md
    - .planning/phases/04-acquisition-execution/VERIFICATION.md
    - .planning/phases/06-cli-productization/06-VERIFICATION.md
  </read_first>
  <files>.planning/phases/05-export-artifacts/05-VERIFICATION.md</files>
  <action>Author `.planning/phases/05-export-artifacts/05-VERIFICATION.md` in the same durable style used by the existing verification artifacts. Include the Phase 5 goal, requirement list, `## Verdict`, a clear statement that export validation/UAT are evidence inputs, and a requirement-by-requirement assessment for `EXP-01` through `EXP-04`. The `EXP-01` section must cite successful VCD artifact creation and post-export device reuse. The `EXP-02` section must cite both synthetic VCD timing/channel evidence and the manual real-hardware plausibility checks, while preserving the Nyquist-safe caution. The `EXP-03` section must cite metadata sidecar generation plus CLI artifact reporting. The `EXP-04` section must enumerate the required metadata fields from the requirement text and tie each field family to the existing observed-fact evidence in validation/UAT. End the file with explicit pass decisions for all four `EXP-*` requirements and a final instruction to rerun `/gsd:audit-milestone` instead of editing the audit by hand.</action>
  <acceptance_criteria>
    - `grep -n "^## Verdict$" /home/seasonyuu/projects/dsview-cli/.planning/phases/05-export-artifacts/05-VERIFICATION.md` returns a match
    - `grep -n "^## Requirement-by-requirement assessment$" /home/seasonyuu/projects/dsview-cli/.planning/phases/05-export-artifacts/05-VERIFICATION.md` returns a match
    - `grep -n "^### EXP-01$" /home/seasonyuu/projects/dsview-cli/.planning/phases/05-export-artifacts/05-VERIFICATION.md` returns a match
    - `grep -n "^### EXP-02$" /home/seasonyuu/projects/dsview-cli/.planning/phases/05-export-artifacts/05-VERIFICATION.md` returns a match
    - `grep -n "^### EXP-03$" /home/seasonyuu/projects/dsview-cli/.planning/phases/05-export-artifacts/05-VERIFICATION.md` returns a match
    - `grep -n "^### EXP-04$" /home/seasonyuu/projects/dsview-cli/.planning/phases/05-export-artifacts/05-VERIFICATION.md` returns a match
    - `grep -n "^Verification input: \.planning/phases/05-export-artifacts/05-VALIDATION.md$" /home/seasonyuu/projects/dsview-cli/.planning/phases/05-export-artifacts/05-VERIFICATION.md` returns a match
    - `grep -n "^Verification input: \.planning/phases/05-export-artifacts/05-UAT.md$" /home/seasonyuu/projects/dsview-cli/.planning/phases/05-export-artifacts/05-VERIFICATION.md` returns a match
    - `grep -n "Nyquist" /home/seasonyuu/projects/dsview-cli/.planning/phases/05-export-artifacts/05-VERIFICATION.md` returns a match inside the `EXP-02` section
    - `grep -n "device model" /home/seasonyuu/projects/dsview-cli/.planning/phases/05-export-artifacts/05-VERIFICATION.md` returns a match inside the `EXP-04` section
    - `grep -n "enabled channels" /home/seasonyuu/projects/dsview-cli/.planning/phases/05-export-artifacts/05-VERIFICATION.md` returns a match inside the `EXP-04` section
    - `grep -n "sample rate" /home/seasonyuu/projects/dsview-cli/.planning/phases/05-export-artifacts/05-VERIFICATION.md` returns a match inside the `EXP-04` section
    - `grep -n "sample limit or actual sample count" /home/seasonyuu/projects/dsview-cli/.planning/phases/05-export-artifacts/05-VERIFICATION.md` returns a match inside the `EXP-04` section
    - `grep -n "capture timestamp" /home/seasonyuu/projects/dsview-cli/.planning/phases/05-export-artifacts/05-VERIFICATION.md` returns a match inside the `EXP-04` section
    - `grep -n "tool version" /home/seasonyuu/projects/dsview-cli/.planning/phases/05-export-artifacts/05-VERIFICATION.md` returns a match inside the `EXP-04` section
    - `grep -n "^Closure marker: EXP-01 | PASS |" /home/seasonyuu/projects/dsview-cli/.planning/phases/05-export-artifacts/05-VERIFICATION.md` returns a match
    - `grep -n "^Closure marker: EXP-02 | PASS |" /home/seasonyuu/projects/dsview-cli/.planning/phases/05-export-artifacts/05-VERIFICATION.md` returns a match
    - `grep -n "^Closure marker: EXP-03 | PASS |" /home/seasonyuu/projects/dsview-cli/.planning/phases/05-export-artifacts/05-VERIFICATION.md` returns a match
    - `grep -n "^Closure marker: EXP-04 | PASS |" /home/seasonyuu/projects/dsview-cli/.planning/phases/05-export-artifacts/05-VERIFICATION.md` returns a match
    - `grep -n "/gsd:audit-milestone" /home/seasonyuu/projects/dsview-cli/.planning/phases/05-export-artifacts/05-VERIFICATION.md` returns a match
    - `grep -n "\.planning/v1\.0-MILESTONE-AUDIT\.md" /home/seasonyuu/projects/dsview-cli/.planning/phases/05-export-artifacts/05-VERIFICATION.md` returns no matches
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Reconcile export requirement rows only after the verification artifact exists</name>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/05-export-artifacts/05-VERIFICATION.md
    - .planning/v1.0-MILESTONE-AUDIT.md
    - .planning/phases/08-verification-backfill-for-acquisition-and-export/08-01-PLAN.md
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>After `.planning/phases/05-export-artifacts/05-VERIFICATION.md` exists, update only the `EXP-01`, `EXP-02`, `EXP-03`, and `EXP-04` traceability rows in `.planning/REQUIREMENTS.md` so they point to the new Phase 5 verification artifact and require a fresh `/gsd:audit-milestone` rerun. Do not reopen or rewrite the reconciled `RUN-*` rows from `08-01`, and do not change unrelated `DEV-*`, `CAP-*`, or `CLI-*` rows.</action>
  <acceptance_criteria>
    - `grep -n "^| EXP-01 | Phase 8 | Closed via \.planning/phases/05-export-artifacts/05-VERIFICATION.md; rerun `/gsd:audit-milestone` |$" /home/seasonyuu/projects/dsview-cli/.planning/REQUIREMENTS.md` returns a match
    - `grep -n "^| EXP-02 | Phase 8 | Closed via \.planning/phases/05-export-artifacts/05-VERIFICATION.md; rerun `/gsd:audit-milestone` |$" /home/seasonyuu/projects/dsview-cli/.planning/REQUIREMENTS.md` returns a match
    - `grep -n "^| EXP-03 | Phase 8 | Closed via \.planning/phases/05-export-artifacts/05-VERIFICATION.md; rerun `/gsd:audit-milestone` |$" /home/seasonyuu/projects/dsview-cli/.planning/REQUIREMENTS.md` returns a match
    - `grep -n "^| EXP-04 | Phase 8 | Closed via \.planning/phases/05-export-artifacts/05-VERIFICATION.md; rerun `/gsd:audit-milestone` |$" /home/seasonyuu/projects/dsview-cli/.planning/REQUIREMENTS.md` returns a match
    - `grep -n "^| RUN-01 | Phase 8 | Closed via \.planning/phases/04-acquisition-execution/VERIFICATION.md; rerun `/gsd:audit-milestone` |$" /home/seasonyuu/projects/dsview-cli/.planning/REQUIREMENTS.md` returns a match
    - `grep -n "^| DEV-01 | Phase 7 | Closed via \.planning/phases/02-device-discovery-and-session-bring-up/02-VERIFICATION.md and \.planning/phases/02-device-discovery-and-session-bring-up/02-VALIDATION.md; rerun `/gsd:audit-milestone` |$" /home/seasonyuu/projects/dsview-cli/.planning/REQUIREMENTS.md` returns a match
    - `grep -n "^| CAP-04 | Phase 7 | Closed via \.planning/phases/03-capture-configuration-surface/03-VERIFICATION.md and \.planning/phases/03-capture-configuration-surface/03-VALIDATION.md; rerun `/gsd:audit-milestone` |$" /home/seasonyuu/projects/dsview-cli/.planning/REQUIREMENTS.md` returns a match
    - `grep -n "^| CLI-03 | Phase 9 | Pending |$" /home/seasonyuu/projects/dsview-cli/.planning/REQUIREMENTS.md` returns a match
  </acceptance_criteria>
</task>

<verification>
- Read `.planning/phases/05-export-artifacts/05-VERIFICATION.md` and confirm it contains `## Verdict`, `## Requirement-by-requirement assessment`, `### EXP-01` through `### EXP-04`, and `Closure marker: EXP-0X | PASS | ...` lines for all four export requirements.
- Confirm `.planning/phases/05-export-artifacts/05-VERIFICATION.md` contains exact `Verification input:` lines for both `.planning/phases/05-export-artifacts/05-VALIDATION.md` and `.planning/phases/05-export-artifacts/05-UAT.md`.
- Confirm the `EXP-02` verification text preserves Nyquist-safe timing/plausibility caveats and the `EXP-04` verification text stays grounded in observed metadata facts.
- Read `.planning/REQUIREMENTS.md` and confirm the `EXP-01` through `EXP-04` rows exactly match `Closed via .planning/phases/05-export-artifacts/05-VERIFICATION.md; rerun `/gsd:audit-milestone``, while the `RUN-*` rows still match the `08-01` closure text.
- Confirm the Phase 8 handoff requires a fresh `/gsd:audit-milestone` rerun and leaves `.planning/v1.0-MILESTONE-AUDIT.md` untouched.
</verification>
