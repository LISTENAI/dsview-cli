---
phase: 06-cli-productization
plan: 03
title: Add process-boundary CLI validation and release-style workflow checks for capture export
subsystem: test
tags: [tests, validation, cli, process, help, exit-codes, shell, uat, dslogic-plus]
wave: 2
autonomous: true
depends_on: [06-02]
requirements: [CLI-01, CLI-02, CLI-03]
requirements_addressed: [CLI-01, CLI-02, CLI-03]
files_modified:
  - crates/dsview-cli/tests/capture_cli.rs
  - crates/dsview-cli/src/main.rs
  - .planning/phases/06-cli-productization/06-VALIDATION.md
  - .planning/phases/06-cli-productization/06-RESEARCH.md
must_haves:
  truths:
    - Phase 6 proof must exercise the real CLI boundary rather than relying only on helper-level tests and internal result shaping.
    - Validation covers help text, stdout/stderr behavior, exit codes, artifact-path reporting, and representative success/failure cases.
    - Manual UAT confirms that the command works as a non-interactive shell workflow on current DSLogic Plus hardware rather than only as a library path.
    - Phase 6 is not complete until both the automation-facing JSON path and the human-facing shell path are verified against the final command surface.
  artifacts:
    - crates/dsview-cli/tests/capture_cli.rs
    - crates/dsview-cli/src/main.rs
    - .planning/phases/06-cli-productization/06-VALIDATION.md
    - .planning/phases/06-cli-productization/06-RESEARCH.md
  key_links:
    - from: crates/dsview-cli/tests/capture_cli.rs
      to: crates/dsview-cli/src/main.rs
      via: spawned-process checks for the final command surface, help text, and output behavior
    - from: .planning/phases/06-cli-productization/06-VALIDATION.md
      to: manual DSLogic Plus UAT
      via: final release-style shell workflow sign-off for the polished capture command
---

<objective>
Prove that the polished capture command works as a real CLI product by validating its final process-boundary behavior, output, and release-style shell workflow on both automated and manual paths.
</objective>

<scope>
This plan covers spawned-process CLI tests, help and usage verification, process-boundary artifact checks, and final manual shell workflow UAT. It does not redesign the command surface again.
</scope>

<task type="auto">
  <name>Move Phase 6 CLI proof to the spawned-process boundary</name>
  <read_first>
    - crates/dsview-cli/tests/capture_cli.rs
    - crates/dsview-cli/src/main.rs
    - .planning/phases/06-cli-productization/06-VALIDATION.md
  </read_first>
  <files>crates/dsview-cli/tests/capture_cli.rs, .planning/phases/06-cli-productization/06-VALIDATION.md</files>
  <action>Add or restructure tests so the final command is exercised through the compiled CLI boundary wherever practical. Prefer checks that parse stdout, inspect stderr, and assert process exit status over tests that only instantiate helper structs or call classifier functions directly.</action>
  <acceptance_criteria>
    - automated coverage includes spawned-process CLI checks rather than only internal helper assertions
    - tests assert stdout/stderr separation and exit status behavior
    - tests cover both success and representative failure behavior at the real CLI boundary
    - the validation doc points at concrete commands for these checks
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Verify help text, format behavior, and final command discoverability</name>
  <read_first>
    - crates/dsview-cli/src/main.rs
    - crates/dsview-cli/tests/capture_cli.rs
    - .planning/phases/06-cli-productization/06-RESEARCH.md
  </read_first>
  <files>crates/dsview-cli/src/main.rs, crates/dsview-cli/tests/capture_cli.rs, .planning/phases/06-cli-productization/06-RESEARCH.md</files>
  <action>Make the final command surface discoverable without outside tribal knowledge. Validate that help text explains runtime-selection requirements, output-path controls, and text-vs-JSON behavior clearly enough for shell users and agent workflows to use the command directly.</action>
  <acceptance_criteria>
    - `--help` output documents the final path controls and output-format behavior clearly
    - runtime-selection requirements remain visible and actionable in help text
    - tests lock at least the key help-text fragments needed to discover the product surface
    - the research artifact records any intentional caveats that remain part of the v1 CLI UX
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Validate artifact creation and reported paths against real filesystem outcomes</name>
  <read_first>
    - crates/dsview-cli/tests/capture_cli.rs
    - crates/dsview-core/tests/export_artifacts.rs
    - .planning/phases/06-cli-productization/06-VALIDATION.md
  </read_first>
  <files>crates/dsview-cli/tests/capture_cli.rs, crates/dsview-core/tests/export_artifacts.rs, .planning/phases/06-cli-productization/06-VALIDATION.md</files>
  <action>Add process-level checks that create artifacts in temp locations, then verify that the paths printed by the CLI match the files actually present after the command succeeds. Keep the checks focused on the final product behavior rather than re-proving every sys/core internal from earlier phases.</action>
  <acceptance_criteria>
    - at least one automated check proves the reported VCD path matches the created VCD file
    - at least one automated check proves the reported metadata path matches the created JSON file
    - checks remain cleanup-safe and do not depend on persistent local filesystem state
    - representative non-success paths still avoid presenting misleading artifact success output
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Record final manual shell-workflow UAT for DSLogic Plus</name>
  <read_first>
    - .planning/STATE.md
    - .planning/phases/06-cli-productization/06-VALIDATION.md
    - crates/dsview-cli/src/main.rs
  </read_first>
  <files>.planning/phases/06-cli-productization/06-VALIDATION.md, .planning/phases/06-cli-productization/06-RESEARCH.md</files>
  <action>Run and record a final non-interactive shell workflow using connected `DSLogic Plus` hardware and the source-runtime path already validated by earlier phases. Confirm the polished command accepts the chosen path controls, reports the final artifact set clearly, exits correctly, and remains reusable for a second run on the same environment.</action>
  <acceptance_criteria>
    - manual UAT uses the final Phase 6 command surface rather than an older helper-oriented invocation pattern
    - a successful run produces both artifacts at the requested or derived final paths and reports them clearly
    - the command remains non-interactive and automation-friendly under the validated shell workflow
    - immediate rerun after success still works on current hardware
    - completion evidence is recorded explicitly in the validation artifact so Phase 6 sign-off is not implied
  </acceptance_criteria>
  <gate>
    - Do not mark Phase 6 complete until the final manual DSLogic Plus shell-workflow UAT is executed and recorded green.
  </gate>
</task>

<verification>
- Run `cargo test -p dsview-cli`
- Run `cargo test --workspace`
- Inspect `--help` output for the final capture command and confirm runtime, format, and path controls are all discoverable
- Execute one manual DSLogic Plus shell workflow using the final command surface and confirm artifact reporting, exit status, and immediate rerun behavior on the current machine
</verification>
