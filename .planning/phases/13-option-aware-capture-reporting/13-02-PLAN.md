---
phase: 13-option-aware-capture-reporting
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - crates/dsview-core/src/lib.rs
  - crates/dsview-core/tests/export_artifacts.rs
  - crates/dsview-cli/src/main.rs
autonomous: true
requirements:
  - RUN-05
must_haves:
  truths:
    - "Successful runs record both requested and effective device-option values in machine-readable outputs."
    - "Text output stays concise and shows only the effective values actually used for the run."
    - "Requested and effective values come from one shared core model so CLI JSON and metadata cannot drift independently."
  artifacts:
    - path: crates/dsview-core/src/lib.rs
      provides: "Shared requested/effective device-option facts for metadata and CLI success output"
      contains: "CaptureDeviceOptionFacts"
    - path: crates/dsview-core/tests/export_artifacts.rs
      provides: "Schema and metadata regressions for requested/effective option reporting"
      contains: "metadata_sidecar_includes_requested_and_effective_device_options"
    - path: crates/dsview-cli/src/main.rs
      provides: "JSON and text success rendering for effective/requested device-option facts"
      contains: "render_capture_success_text_lists_effective_device_options"
  key_links:
    - from: crates/dsview-core/src/lib.rs
      to: crates/dsview-cli/src/main.rs
      via: "CLI response reuses the core requested/effective facts instead of duplicating serialization logic"
      pattern: "device_options"
    - from: crates/dsview-core/src/lib.rs
      to: crates/dsview-core/tests/export_artifacts.rs
      via: "metadata schema assertions lock the exact requested/effective block"
      pattern: "requested_and_effective"
---

<objective>
Persist the requested and effective DSLogic Plus option facts in the capture artifacts, then expose the same facts through concise text output and explicit JSON success responses.

Purpose: Applying options at runtime is not enough for automation; successful runs must preserve what was requested and what the device actually used without forcing consumers to parse human text.
Output: One shared core reporting model reused by metadata and CLI success output, with text focused on effective values and JSON carrying both requested and effective facts.
</objective>

<execution_context>
@/home/seasonyuu/.codex/get-shit-done/workflows/execute-plan.md
@/home/seasonyuu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-option-aware-capture-reporting/13-CONTEXT.md
@.planning/phases/13-option-aware-capture-reporting/13-RESEARCH.md
@.planning/phases/13-option-aware-capture-reporting/13-VALIDATION.md
@.planning/phases/12-cli-device-option-surface/12-03-SUMMARY.md
@CLAUDE.md
@crates/dsview-core/src/lib.rs
@crates/dsview-core/tests/export_artifacts.rs
@crates/dsview-cli/src/main.rs

<interfaces>
From `crates/dsview-core/src/lib.rs` today:

```rust
pub struct CaptureMetadata {
    pub schema_version: u32,
    pub tool: MetadataToolInfo,
    pub capture: MetadataCaptureInfo,
    pub acquisition: MetadataAcquisitionInfo,
    pub artifacts: MetadataArtifactInfo,
}

pub struct CaptureExportSuccess {
    pub vcd_path: PathBuf,
    pub metadata_path: PathBuf,
    pub export: VcdExportFacts,
    pub metadata: CaptureMetadata,
}
```

From `crates/dsview-cli/src/main.rs` today:

```rust
struct CaptureResponse {
    selected_handle: u64,
    completion: &'static str,
    saw_logic_packet: bool,
    saw_end_packet: bool,
    saw_terminal_normal_end: bool,
    cleanup_succeeded: bool,
    artifacts: CaptureArtifactsResponse,
}
```

Plan `13-02` must add one shared requested/effective device-option facts model in core and reuse it in both metadata and CLI success output. Do not create separate metadata-only and CLI-only representations.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a shared requested/effective device-option facts model to capture metadata</name>
  <files>crates/dsview-core/src/lib.rs, crates/dsview-core/tests/export_artifacts.rs</files>
  <read_first>
    - .planning/phases/13-option-aware-capture-reporting/13-CONTEXT.md
    - .planning/phases/13-option-aware-capture-reporting/13-RESEARCH.md
    - .planning/phases/13-option-aware-capture-reporting/13-VALIDATION.md
    - crates/dsview-core/src/lib.rs
    - crates/dsview-core/tests/export_artifacts.rs
  </read_first>
  <behavior>
    - Metadata includes both the full requested option set and the effective values read back after apply, using explicit stable IDs and numeric fields instead of human-only labels.
    - The metadata schema changes are versioned so downstream consumers can distinguish the new required `device_options` block.
    - Successful runs with no validated Phase 12 request still emit inherited effective device-option facts for the baseline path.
  </behavior>
  <action>In `crates/dsview-core/src/lib.rs`, add serde-ready structs named `CaptureDeviceOptionFacts` and `CaptureDeviceOptionSnapshot` with these exact fields in both `requested` and `effective`: `operation_mode_id`, `stop_option_id`, `channel_mode_id`, `enabled_channels`, `threshold_volts`, `filter_id`, `sample_rate_hz`, and `sample_limit`. Bump `schema_version` from `1` to `2` because the sidecar now contains a required `device_options` reporting contract for automation. Source `requested` from the full validated request that Phase 13 applies per D-09. For option-aware runs, source `effective` from the post-apply effective state produced by Plan 13-01: map readback codes back to stable IDs via the validated capability catalog/current snapshot, and take `effective.enabled_channels` from the successfully applied validated request after the channel-enable step succeeds. For baseline runs where `validated_device_options == None`, add an explicit inherited-facts path that builds `effective` device-option facts from the current device snapshot plus current runtime sample settings, and leave `requested` absent or mirrored according to the shared core contract you define consistently. Extend `CaptureExportRequest`, `CaptureMetadata`, and the export path to carry that block. In `crates/dsview-core/tests/export_artifacts.rs`, add tests named `metadata_sidecar_includes_requested_and_effective_device_options`, `metadata_schema_version_is_2_when_device_option_facts_are_present`, `requested_and_effective_sample_limit_values_can_differ`, and `baseline_capture_metadata_includes_inherited_effective_device_options` to lock both the option-aware and no-overrides contracts.</action>
  <acceptance_criteria>
    - `rg -n "struct CaptureDeviceOptionFacts|struct CaptureDeviceOptionSnapshot|requested: CaptureDeviceOptionSnapshot|effective: CaptureDeviceOptionSnapshot" crates/dsview-core/src/lib.rs` finds the shared reporting structs required by D-07 and D-08.
    - `rg -n "schema_version: 2|device_options: CaptureDeviceOptionFacts" crates/dsview-core/src/lib.rs` finds the metadata schema update.
    - `rg -n "metadata_sidecar_includes_requested_and_effective_device_options|metadata_schema_version_is_2_when_device_option_facts_are_present|requested_and_effective_sample_limit_values_can_differ|baseline_capture_metadata_includes_inherited_effective_device_options" crates/dsview-core/tests/export_artifacts.rs` finds the new export regressions.
    - `rg -n "\"device_options\"|\"requested\"|\"effective\"|\"sample_limit\"" crates/dsview-core/tests/export_artifacts.rs` shows the sidecar assertions cover the nested requested/effective facts.
    - `rg -n "validated_device_options == None|baseline|inherited effective" .planning/phases/13-option-aware-capture-reporting/13-02-PLAN.md` shows the baseline reporting path is explicitly planned.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p dsview-core --test export_artifacts -- --nocapture</automated>
  </verify>
  <done>The metadata sidecar now records explicit requested/effective DSLogic Plus option facts from one shared core model, and the schema version reflects the new required block.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reuse the shared facts in CLI JSON and keep text output focused on effective values</name>
  <files>crates/dsview-cli/src/main.rs</files>
  <read_first>
    - .planning/phases/13-option-aware-capture-reporting/13-CONTEXT.md
    - .planning/phases/13-option-aware-capture-reporting/13-RESEARCH.md
    - .planning/phases/13-option-aware-capture-reporting/13-VALIDATION.md
    - crates/dsview-cli/src/main.rs
    - crates/dsview-core/src/lib.rs
  </read_first>
  <behavior>
    - JSON success output includes both requested and effective device-option facts from the same core data that metadata writes.
    - Text success output stays concise, showing only the effective values actually used for the run and not duplicating a requested block.
  </behavior>
  <action>Extend `CaptureResponse` in `crates/dsview-cli/src/main.rs` with a `device_options` field that reuses the core `CaptureDeviceOptionFacts` model from the export result instead of re-serializing ad hoc maps. Update `run_capture` so successful captures populate that field from `export.metadata.device_options`. Keep JSON authoritative. For text mode, add one compact `effective options:` section immediately before the artifact paths, using the fixed D-08 order: operation mode, stop option, channel mode, enabled channels, threshold volts, filter, sample rate, sample limit. Do not print requested values in text per D-06. Add binary unit tests named `capture_response_json_includes_requested_and_effective_device_options` and `render_capture_success_text_lists_effective_device_options` that assert the JSON payload carries both blocks while text contains only the effective section and still includes artifact paths.</action>
  <acceptance_criteria>
    - `rg -n "device_options: CaptureDeviceOptionFacts|export\\.metadata\\.device_options" crates/dsview-cli/src/main.rs` finds the shared-facts reuse from core into CLI success output.
    - `rg -n "effective options:" crates/dsview-cli/src/main.rs` finds the concise text summary required by D-06.
    - `rg -n "capture_response_json_includes_requested_and_effective_device_options|render_capture_success_text_lists_effective_device_options" crates/dsview-cli/src/main.rs` finds the direct CLI regression coverage.
    - `! rg -n "requested options:" crates/dsview-cli/src/main.rs` succeeds, proving text mode stays focused on effective values only.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p dsview-cli --bin dsview-cli -- --nocapture && cargo test -p dsview-core --test export_artifacts -- --nocapture</automated>
  </verify>
  <done>The CLI success contract now exposes explicit requested/effective device-option facts in JSON while keeping text mode concise and effective-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Core apply/report model -> metadata sidecar | Automation will trust the sidecar as the authoritative record of what was requested and what actually happened |
| Core apply/report model -> CLI success JSON | The CLI must not invent a second representation that can drift from metadata |
| CLI success text -> human operator | Text should remain concise and truthful without becoming the only place where effective values exist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | I | `crates/dsview-core/src/lib.rs` metadata builder | mitigate | Build one `CaptureDeviceOptionFacts` struct in core and serialize it into both metadata and CLI success output |
| T-13-02-02 | R | `crates/dsview-cli/src/main.rs` text rendering | mitigate | Keep text effective-only and derive it from the same shared facts block so operators are not shown stale or divergent values |
| T-13-02-03 | T | `crates/dsview-core/tests/export_artifacts.rs` schema contract | mitigate | Bump `schema_version` to `2` and add explicit requested/effective regression assertions so consumers can detect the new required block |
</threat_model>

<verification>
- `cargo test -p dsview-core --test export_artifacts -- --nocapture`
- `cargo test -p dsview-cli --bin dsview-cli -- --nocapture`
</verification>

<success_criteria>
- Metadata and CLI JSON both include explicit requested/effective option facts from one shared core model
- Text output remains concise and reports only effective values actually used for the run
- The metadata schema version and regression tests make the new reporting contract explicit for automation consumers
</success_criteria>

<output>
After completion, create `.planning/phases/13-option-aware-capture-reporting/13-02-SUMMARY.md`
</output>
