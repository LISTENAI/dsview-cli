---
phase: 13-option-aware-capture-reporting
plan: 03
type: execute
wave: 3
depends_on:
  - 13-01
  - 13-02
files_modified:
  - crates/dsview-cli/src/main.rs
  - crates/dsview-cli/tests/capture_cli.rs
  - .planning/phases/13-option-aware-capture-reporting/13-VALIDATION.md
autonomous: true
requirements:
  - RUN-04
  - RUN-05
must_haves:
  truths:
    - "Spawned CLI tests prove option-aware success, partial-apply failure, and inherited/default runs all behave deterministically."
    - "The debug-only test seam remains env-gated and is reused instead of adding a new user-facing testing switch."
    - "The phase validation artifact tells the next human verifier exactly how to prove the final runtime/reporting behavior on real hardware."
  artifacts:
    - path: crates/dsview-cli/tests/capture_cli.rs
      provides: "End-to-end CLI regression coverage for success, failure, and baseline option-aware reporting"
      contains: "capture_json_success_reports_requested_and_effective_device_options"
    - path: .planning/phases/13-option-aware-capture-reporting/13-VALIDATION.md
      provides: "Updated automated and hardware verification contract for the shipped Phase 13 behavior"
      contains: "capture_json_success_reports_requested_and_effective_device_options"
  key_links:
    - from: crates/dsview-cli/tests/capture_cli.rs
      to: crates/dsview-cli/src/main.rs
      via: "spawned binary assertions over the env-gated option-aware fixture path"
      pattern: "DSVIEW_CLI_TEST_DEVICE_OPTIONS_FIXTURE"
    - from: .planning/phases/13-option-aware-capture-reporting/13-VALIDATION.md
      to: crates/dsview-cli/tests/capture_cli.rs
      via: "final automated commands and manual checklist reflect the shipped CLI contract"
      pattern: "capture_apply_failure_reports_applied_steps_and_failed_step"
---

<objective>
Lock the final public capture contract with spawned regressions, then refresh the validation artifact so manual hardware verification matches the shipped option-aware behavior exactly.

Purpose: Phase 13 changes runtime execution, success JSON/text, and error reporting. The final guardrail must exercise the compiled binary and leave a truthful hardware-verification checklist for a machine with working DSLogic Plus access.
Output: Spawned CLI regression coverage for success and partial-apply failures, plus an updated Phase 13 validation contract that names the final automated commands and manual hardware steps.
</objective>

<execution_context>
@/home/seasonyuu/.codex/get-shit-done/workflows/execute-plan.md
@/home/seasonyuu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-option-aware-capture-reporting/13-CONTEXT.md
@.planning/phases/13-option-aware-capture-reporting/13-RESEARCH.md
@.planning/phases/13-option-aware-capture-reporting/13-VALIDATION.md
@CLAUDE.md
@crates/dsview-cli/src/main.rs
@crates/dsview-cli/tests/capture_cli.rs
@crates/dsview-core/src/lib.rs

<interfaces>
From `crates/dsview-cli/src/main.rs` today:

```rust
const TEST_DEVICE_OPTIONS_FIXTURE_ENV: &str = "DSVIEW_CLI_TEST_DEVICE_OPTIONS_FIXTURE";

fn capture_test_fixture(
    handle: SelectionHandle,
) -> Option<(DeviceOptionsSnapshot, DeviceOptionValidationCapabilities)>;
```

From `crates/dsview-cli/tests/capture_cli.rs` today:

```rust
fn mock_cli_command() -> Command {
    let mut command = cli_command();
    command.env("DSVIEW_CLI_TEST_DEVICE_OPTIONS_FIXTURE", "1");
    command
}
```

Plan `13-03` must keep the test seam debug-only and env-gated. Reuse `DSVIEW_CLI_TEST_DEVICE_OPTIONS_FIXTURE` instead of adding another public flag or hidden CLI argument.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add spawned CLI regressions for option-aware success, partial-apply failure, and inherited baseline runs</name>
  <files>crates/dsview-cli/src/main.rs, crates/dsview-cli/tests/capture_cli.rs</files>
  <read_first>
    - .planning/phases/13-option-aware-capture-reporting/13-CONTEXT.md
    - .planning/phases/13-option-aware-capture-reporting/13-RESEARCH.md
    - .planning/phases/13-option-aware-capture-reporting/13-VALIDATION.md
    - crates/dsview-cli/src/main.rs
    - crates/dsview-cli/tests/capture_cli.rs
    - crates/dsview-core/src/lib.rs
  </read_first>
  <behavior>
    - The compiled CLI can prove the final success JSON/text contract and the final partial-apply failure shape without relying on live hardware.
    - The env-gated fixture remains debug-only and is extended only as much as needed to drive the option-aware runtime/reporting states through the real binary.
  </behavior>
  <action>Expand the existing `DSVIEW_CLI_TEST_DEVICE_OPTIONS_FIXTURE` seam in `crates/dsview-cli/src/main.rs` only if Plan 13-01 did not already make it capable of simulating option-aware success and partial-apply failure. Do not add a new user-facing flag. In `crates/dsview-cli/tests/capture_cli.rs`, add spawned tests named `capture_json_success_reports_requested_and_effective_device_options`, `capture_text_success_reports_effective_device_options_concisely`, `capture_apply_failure_reports_applied_steps_and_failed_step`, and `capture_without_overrides_reports_inherited_effective_device_options`. The success cases must assert the compiled binary writes requested/effective device-option fields in JSON and only effective values in text. The failure case must assert the error includes both `applied_steps` and `failed_step` per D-03. The inherited baseline case must omit the Phase 12 flags, still succeed, and prove the output keeps the existing completion/artifact fields while now reporting the inherited effective option facts. Re-run `devices_cli` unchanged in verification to prove the discovery contract from Phase 12 did not drift.</action>
  <acceptance_criteria>
    - `rg -n "capture_json_success_reports_requested_and_effective_device_options|capture_text_success_reports_effective_device_options_concisely|capture_apply_failure_reports_applied_steps_and_failed_step|capture_without_overrides_reports_inherited_effective_device_options" crates/dsview-cli/tests/capture_cli.rs` finds the final spawned CLI cases.
    - `rg -n "\"requested\"|\"effective\"|\"applied_steps\"|\"failed_step\"|effective options:" crates/dsview-cli/tests/capture_cli.rs` shows the tests lock the success and failure reporting contract.
    - `rg -n "DSVIEW_CLI_TEST_DEVICE_OPTIONS_FIXTURE" crates/dsview-cli/src/main.rs crates/dsview-cli/tests/capture_cli.rs` confirms the existing env-gated seam is reused instead of adding a new CLI switch.
    - `! rg -n "TEST_CAPTURE_RUNTIME_FIXTURE|--test-fixture" crates/dsview-cli/src/main.rs crates/dsview-cli/tests/capture_cli.rs` succeeds, proving no second public or hidden fixture entrypoint was added.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p dsview-cli --test capture_cli -- --nocapture && cargo test -p dsview-cli --test devices_cli -- --nocapture</automated>
  </verify>
  <done>The compiled CLI contract is locked for option-aware success, partial-apply failure, and inherited/default runs, all through the existing env-gated fixture seam.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh the Phase 13 validation artifact with the final automated and hardware checks</name>
  <files>.planning/phases/13-option-aware-capture-reporting/13-VALIDATION.md</files>
  <read_first>
    - .planning/phases/13-option-aware-capture-reporting/13-VALIDATION.md
    - .planning/phases/13-option-aware-capture-reporting/13-CONTEXT.md
    - .planning/phases/13-option-aware-capture-reporting/13-RESEARCH.md
    - crates/dsview-cli/tests/capture_cli.rs
    - crates/dsview-core/tests/export_artifacts.rs
  </read_first>
  <acceptance_criteria>
    - `rg -n "capture_json_success_reports_requested_and_effective_device_options|capture_apply_failure_reports_applied_steps_and_failed_step|cargo test -p dsview-cli --test capture_cli -- --nocapture|cargo test --workspace -- --nocapture" .planning/phases/13-option-aware-capture-reporting/13-VALIDATION.md` finds the refreshed automated verification contract.
    - `rg -n "requested and effective|schema_version|On a machine with a working DSLogic Plus" .planning/phases/13-option-aware-capture-reporting/13-VALIDATION.md` finds the manual hardware checklist aligned to the shipped Phase 13 output fields.
    - `! rg -n "wave_0_complete: false" .planning/phases/13-option-aware-capture-reporting/13-VALIDATION.md` succeeds once the planned automation gaps are closed and the validation artifact is updated truthfully.
  </acceptance_criteria>
  <action>Update `.planning/phases/13-option-aware-capture-reporting/13-VALIDATION.md` after Task 1 lands so it reflects the final shipped commands, test names, and manual hardware expectations. Replace the current Wave 0 placeholders with the completed automated checks from Plans 13-01 through 13-03, keep the per-task verification map aligned to the actual final commands, and rewrite the manual-only verification section so it explicitly checks requested/effective reporting, partial-apply failure honesty, and the metadata `schema_version`/`device_options` block on a machine with working DSLogic Plus access. Do not mark the hardware step complete on this machine if libusb access is still unavailable; leave it as a truthful follow-up requirement for `/gsd-verify-work`.</action>
  <verify>
    <automated>cargo test -p dsview-cli --test capture_cli -- --nocapture && cargo test -p dsview-cli --test devices_cli -- --nocapture && cargo test --workspace -- --nocapture</automated>
  </verify>
  <done>The Phase 13 validation artifact names the final automated coverage and leaves an accurate hardware-only checklist for the remaining live-device proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Compiled CLI binary -> user scripts | Public success and failure output is now part of the automation contract |
| Debug-only fixture seam -> release binary behavior | Test-only scaffolding must not escape into a normal user-visible switch |
| Validation artifact -> future verifier | The human hardware checklist must match the shipped CLI/metadata contract exactly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03-01 | R | `crates/dsview-cli/tests/capture_cli.rs` | mitigate | Assert the compiled binary, not helper functions, for success JSON/text and partial-apply failure reporting |
| T-13-03-02 | E | `crates/dsview-cli/src/main.rs` debug fixture seam | mitigate | Reuse the existing env-gated debug-only seam and explicitly prohibit a new public testing flag |
| T-13-03-03 | I | `.planning/phases/13-option-aware-capture-reporting/13-VALIDATION.md` | mitigate | Refresh the validation artifact after the code lands so the next human verifier sees the real final commands and expected fields |
</threat_model>

<verification>
- `cargo test -p dsview-cli --test capture_cli -- --nocapture`
- `cargo test -p dsview-cli --test devices_cli -- --nocapture`
- `cargo test --workspace -- --nocapture`
</verification>

<success_criteria>
- Spawned CLI tests prove the final option-aware success and failure contracts through the compiled binary
- The existing env-gated fixture seam remains the only test seam used by Phase 13 CLI regressions
- The validation artifact tells a human exactly how to verify the real DSLogic Plus behavior once hardware/libusb access is available
</success_criteria>

<output>
After completion, create `.planning/phases/13-option-aware-capture-reporting/13-03-SUMMARY.md`
</output>
