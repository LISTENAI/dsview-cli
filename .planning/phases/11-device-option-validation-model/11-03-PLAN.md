---
phase: 11-device-option-validation-model
plan: 03
type: execute
wave: 3
depends_on:
  - 11-02
files_modified:
  - crates/dsview-core/tests/device_option_validation.rs
  - crates/dsview-cli/src/main.rs
autonomous: true
requirements:
  - VAL-01
  - VAL-02
must_haves:
  truths:
    - Automated tests lock the DSView-backed mode rules for channel ceilings, sample-rate ceilings, sample-limit capacity, threshold validation, filter membership, and stop-option compatibility
    - CLI tests assert exact stable validation error codes instead of only checking human-readable messages
    - Existing `v1.0` capture/device-option CLI tests still pass after Phase 11 validation changes
  artifacts:
    - path: crates/dsview-core/tests/device_option_validation.rs
      provides: DSView-rule matrix for valid and invalid validation outcomes
      contains: stop_option_incompatible
    - path: crates/dsview-cli/src/main.rs
      provides: CLI unit tests for stable validation error-code mapping
      contains: sample_rate_unsupported_maps_to_stable_validation_error_code
  key_links:
    - from: crates/dsview-core/tests/device_option_validation.rs
      to: crates/dsview-core/src/device_option_validation.rs
      via: capability fixtures calling validate_request
      pattern: validate_request
    - from: crates/dsview-cli/src/main.rs
      to: crates/dsview-core/src/device_option_validation.rs
      via: classify_validation_error tests asserting DeviceOptionValidationError::code
      pattern: classify_validation_error
---

<objective>
Lock Phase 11 behavior with DSView-rule tests so invalid option combinations cannot regress silently and stable validation codes remain machine-readable.

Purpose: This phase is security- and correctness-sensitive; the rule matrix must be executable in CI before Phase 12 starts wiring new CLI flags.
Output: An expanded core validation matrix on top of the existing Phase 11 test target, broader CLI validation-code coverage, and rerun CLI regression suites.
</objective>

<execution_context>
@/home/seasonyuu/.codex/get-shit-done/workflows/execute-plan.md
@/home/seasonyuu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-device-option-validation-model/11-RESEARCH.md
@.planning/phases/11-device-option-validation-model/11-VALIDATION.md
@CLAUDE.md
@crates/dsview-core/src/device_option_validation.rs
@crates/dsview-core/tests/device_options.rs
@crates/dsview-cli/src/main.rs
@crates/dsview-cli/tests/capture_cli.rs
@crates/dsview-cli/tests/device_options_cli.rs
@DSView/libsigrok4DSL/hardware/DSL/dsl.h
@DSView/libsigrok4DSL/hardware/DSL/dsl.c
@DSView/libsigrok4DSL/hardware/DSL/dslogic.c
@DSView/DSView/pv/prop/binding/deviceoptions.cpp
@DSView/NEWS31

<interfaces>
Expected Phase 11 interfaces from Plan `11-02`:

```rust
pub struct DeviceOptionValidationRequest { /* stable ids + numeric fields */ }
pub struct DeviceOptionValidationCapabilities { /* per-mode capability snapshot */ }
pub struct ValidatedDeviceOptionRequest { /* normalized validated selection */ }

pub enum DeviceOptionValidationError {
    /* includes codes such as sample_rate_unsupported and stop_option_incompatible */
}

impl DeviceOptionValidationCapabilities {
    pub fn validate_request(
        &self,
        request: &DeviceOptionValidationRequest,
    ) -> Result<ValidatedDeviceOptionRequest, DeviceOptionValidationError>;
}
```

CLI mapping expected from `crates/dsview-cli/src/main.rs`:

```rust
fn classify_validation_error(error: &DeviceOptionValidationError) -> ErrorResponse;
```

Tests in this plan should exercise those interfaces directly and assert the exact code strings, not just substrings in messages.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the DSView-rule validation matrix in `dsview-core`</name>
  <files>crates/dsview-core/tests/device_option_validation.rs</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/11-device-option-validation-model/11-RESEARCH.md
    - .planning/phases/11-device-option-validation-model/11-VALIDATION.md
    - crates/dsview-core/src/device_option_validation.rs
    - crates/dsview-core/tests/device_options.rs
    - DSView/libsigrok4DSL/hardware/DSL/dsl.h
    - DSView/libsigrok4DSL/hardware/DSL/dsl.c
    - DSView/libsigrok4DSL/hardware/DSL/dslogic.c
    - DSView/DSView/pv/prop/binding/deviceoptions.cpp
    - DSView/NEWS31
  </read_first>
  <behavior>
    - Valid DSLogic Plus combinations return one normalized `ValidatedDeviceOptionRequest`.
    - Invalid DSView combinations fail with the exact Phase 11 code strings.
  </behavior>
  <action>Expand the existing `crates/dsview-core/tests/device_option_validation.rs` target created in `11-01` and seeded in `11-02`. Reuse the same fixture snapshot, but broaden the matrix so it matches the DSView-backed rules from `dsl.h`, `dsl.c`, `dslogic.c`, and `deviceoptions.cpp`. Ensure the expanded target includes at least these exact cases by the end of this plan: `buffer_100x16_accepts_100mhz_and_four_channels`, `buffer_400x4_rejects_five_enabled_channels`, `buffer_100x16_rejects_200mhz_samplerate`, `sample_limit_alignment_can_push_request_over_capacity`, `threshold_value_must_stay_within_vth_range`, `threshold_value_must_follow_point_one_volt_steps`, `filter_id_must_exist_in_supported_list`, and `stream_mode_rejects_buffer_only_stop_option`. Assert exact `DeviceOptionValidationError::code()` values for every failing case, and keep one success case that proves the returned `ValidatedDeviceOptionRequest` preserves both stable IDs and resolved native codes. Keep the tests hardware-free and deterministic.</action>
  <acceptance_criteria>
    - `rg -n "buffer_100x16_accepts_100mhz_and_four_channels|buffer_400x4_rejects_five_enabled_channels|stream_mode_rejects_buffer_only_stop_option" crates/dsview-core/tests/device_option_validation.rs` finds the named DSView-rule cases.
    - `rg -n "sample_rate_unsupported|enabled_channels_exceed_mode_limit|sample_limit_exceeds_capacity|threshold_out_of_range|threshold_step_invalid|filter_unsupported|stop_option_incompatible" crates/dsview-core/tests/device_option_validation.rs` finds the exact asserted validation codes.
    - `cargo test -p dsview-core --test device_option_validation -- --nocapture` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p dsview-core --test device_option_validation -- --nocapture</automated>
  </verify>
  <done>The core validator is locked against the DSView rule matrix for both valid and invalid option combinations required by `VAL-01` and `VAL-02`.</done>
</task>

<task type="auto">
  <name>Task 2: Expand CLI validation-code coverage and rerun the baseline CLI suites</name>
  <files>crates/dsview-cli/src/main.rs</files>
  <read_first>
    - .planning/phases/11-device-option-validation-model/11-RESEARCH.md
    - .planning/phases/11-device-option-validation-model/11-VALIDATION.md
    - crates/dsview-cli/src/main.rs
    - crates/dsview-cli/tests/capture_cli.rs
    - crates/dsview-cli/tests/device_options_cli.rs
    - crates/dsview-core/src/device_option_validation.rs
  </read_first>
  <action>Build on the initial `classify_validation_error()` assertions added in `11-01` and expanded in `11-02`. Extend the existing `#[cfg(test)]` block in `crates/dsview-cli/src/main.rs` so the final Phase 11 coverage includes direct unit tests named `sample_rate_unsupported_maps_to_stable_validation_error_code`, `enabled_channels_exceed_mode_limit_maps_to_stable_validation_error_code`, `sample_limit_exceeds_capacity_maps_to_stable_validation_error_code`, `threshold_out_of_range_maps_to_stable_validation_error_code`, `filter_unsupported_maps_to_stable_validation_error_code`, and `stop_option_incompatible_maps_to_stable_validation_error_code`. Each test must construct the precise `DeviceOptionValidationError` variant, call `classify_validation_error()`, and assert the exact `ErrorResponse.code` string. After those unit tests are green, rerun the existing spawned CLI suites `capture_cli` and `device_options_cli` unchanged to prove the shipped help/output baseline still passes.</action>
  <acceptance_criteria>
    - `rg -n "sample_rate_unsupported_maps_to_stable_validation_error_code|stop_option_incompatible_maps_to_stable_validation_error_code" crates/dsview-cli/src/main.rs` finds the new CLI mapping tests.
    - `rg -n "\"sample_rate_unsupported\"|\"enabled_channels_exceed_mode_limit\"|\"sample_limit_exceeds_capacity\"|\"threshold_out_of_range\"|\"filter_unsupported\"|\"stop_option_incompatible\"" crates/dsview-cli/src/main.rs` finds the exact stable error codes asserted in tests.
    - `cargo test -p dsview-cli --lib -- --nocapture && cargo test -p dsview-cli --test capture_cli -- --nocapture && cargo test -p dsview-cli --test device_options_cli -- --nocapture` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p dsview-cli --lib -- --nocapture && cargo test -p dsview-cli --test capture_cli -- --nocapture && cargo test -p dsview-cli --test device_options_cli -- --nocapture</automated>
  </verify>
  <done>CLI unit tests lock exact validation error codes, and the existing `v1.0`/Phase 10 CLI suites still pass after Phase 11 additions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DSView source-of-truth rules -> repo tests | The test matrix must mirror upstream DSView constraints accurately so regressions are caught before runtime |
| Core validation errors -> CLI test assertions | Exact machine-readable code strings must stay stable across future refactors |
| Phase 11 tests -> `v1.0` CLI baseline | New validation coverage must not break the already shipped CLI help and discovery behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | T/R | `crates/dsview-core/tests/device_option_validation.rs` | mitigate | Encode DSView-derived cases with explicit test names and exact expected codes so rule drift becomes visible in CI |
| T-11-03-02 | R | `crates/dsview-cli/src/main.rs` tests | mitigate | Assert exact `ErrorResponse.code` values for validation failures instead of relying on message text |
| T-11-03-03 | D | CLI regression surface | mitigate | Rerun `capture_cli` and `device_options_cli` after adding Phase 11 tests to prove the shipped baseline still passes |
</threat_model>

<verification>
- `cargo test -p dsview-core --test device_option_validation -- --nocapture`
- `cargo test -p dsview-cli --lib -- --nocapture`
- `cargo test -p dsview-cli --test capture_cli -- --nocapture`
- `cargo test -p dsview-cli --test device_options_cli -- --nocapture`
</verification>

<success_criteria>
- DSView-backed validation rules are covered by deterministic core tests
- CLI tests assert stable validation error codes directly
- Existing CLI regression suites still pass after the Phase 11 validation model lands
</success_criteria>

<output>
After completion, create `.planning/phases/11-device-option-validation-model/11-03-SUMMARY.md`
</output>
