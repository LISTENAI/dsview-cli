---
phase: 11-device-option-validation-model
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - crates/dsview-core/src/device_option_validation.rs
  - crates/dsview-core/src/capture_config.rs
  - crates/dsview-core/src/lib.rs
  - crates/dsview-core/tests/device_option_validation.rs
  - crates/dsview-cli/src/main.rs
autonomous: true
requirements:
  - VAL-01
  - VAL-02
must_haves:
  truths:
    - Unsupported operation-mode, channel-mode, sample-rate, sample-limit, and enabled-channel combinations fail in pure Rust before runtime apply or acquisition begins
    - Unsupported threshold, filter, and mode-incompatible stop-option selections fail with stable machine-readable validation categories
    - CLI validation failures stop mapping to the generic `runtime_error` code when the failure is a known pre-acquisition validation problem
  artifacts:
    - path: crates/dsview-core/src/device_option_validation.rs
      provides: Pure validator and stable validation error taxonomy
      contains: pub enum DeviceOptionValidationError
    - path: crates/dsview-core/src/lib.rs
      provides: Selected-device validation entrypoint for future CLI option wiring
      contains: pub fn validate_device_option_request
    - path: crates/dsview-cli/src/main.rs
      provides: Stable CLI error-code mapping for validation failures
      contains: fn classify_validation_error
    - path: crates/dsview-core/src/capture_config.rs
      provides: Shared sample-limit alignment math reused by the richer validator
      contains: align_sample_limit
  key_links:
    - from: crates/dsview-core/src/lib.rs
      to: crates/dsview-core/src/device_option_validation.rs
      via: Discovery::validate_device_option_request calling DeviceOptionValidationCapabilities::validate_request
      pattern: validate_device_option_request
    - from: crates/dsview-cli/src/main.rs
      to: crates/dsview-core/src/device_option_validation.rs
      via: classify_validation_error mapping DeviceOptionValidationError::code
      pattern: classify_validation_error
---

<objective>
Implement the pure Phase 11 validator and stable error taxonomy that rejects invalid DSLogic Plus option combinations before any acquisition-side work begins.

Purpose: Phase 11 must convert the new request/capability model into deterministic validation results that Phase 12 can call directly and the CLI can report with stable codes.
Output: A pure validation function, the first requirement-specific RED/GREEN cases in the existing Phase 11 test target, and CLI mapping that stops collapsing known validation failures into `runtime_error`.
</objective>

<execution_context>
@/home/seasonyuu/.codex/get-shit-done/workflows/execute-plan.md
@/home/seasonyuu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-device-option-validation-model/11-RESEARCH.md
@.planning/phases/11-device-option-validation-model/11-VALIDATION.md
@CLAUDE.md
@crates/dsview-core/src/device_option_validation.rs
@crates/dsview-core/src/capture_config.rs
@crates/dsview-core/src/device_options.rs
@crates/dsview-core/src/lib.rs
@crates/dsview-cli/src/main.rs
@DSView/libsigrok4DSL/hardware/DSL/dsl.h
@DSView/libsigrok4DSL/hardware/DSL/dsl.c
@DSView/libsigrok4DSL/hardware/DSL/dslogic.c
@DSView/DSView/pv/prop/binding/deviceoptions.cpp
@DSView/NEWS31

<interfaces>
Expected contracts from Plan `11-01`:

```rust
pub struct DeviceOptionValidationRequest {
    pub operation_mode_id: String,
    pub stop_option_id: Option<String>,
    pub channel_mode_id: String,
    pub sample_rate_hz: u64,
    pub sample_limit: u64,
    pub enabled_channels: BTreeSet<u16>,
    pub threshold_volts: Option<f64>,
    pub filter_id: Option<String>,
}

pub struct DeviceOptionValidationCapabilities { /* additive internal snapshot */ }
pub struct ValidatedDeviceOptionRequest { /* resolved stable ids + native codes */ }
```

Current CLI error shape from `crates/dsview-cli/src/main.rs`:

```rust
pub(crate) struct ErrorResponse {
    pub(crate) code: &'static str,
    pub(crate) message: String,
    pub(crate) detail: Option<String>,
    pub(crate) native_error: Option<&'static str>,
    pub(crate) terminal_event: Option<&'static str>,
    pub(crate) cleanup: Option<CaptureCleanupResponse>,
}
```

Current baseline capture path stringifies validation through `RuntimeError::InvalidArgument(error.to_string())`. Phase 11 should replace that for known validation failures.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the pure DSLogic Plus option validator and stable error enum</name>
  <files>crates/dsview-core/src/device_option_validation.rs, crates/dsview-core/src/capture_config.rs, crates/dsview-core/src/lib.rs, crates/dsview-core/tests/device_option_validation.rs</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/11-device-option-validation-model/11-RESEARCH.md
    - crates/dsview-core/src/device_option_validation.rs
    - crates/dsview-core/src/capture_config.rs
    - crates/dsview-core/src/device_options.rs
    - crates/dsview-core/src/lib.rs
    - crates/dsview-core/tests/device_option_validation.rs
    - DSView/libsigrok4DSL/hardware/DSL/dsl.h
    - DSView/libsigrok4DSL/hardware/DSL/dsl.c
    - DSView/libsigrok4DSL/hardware/DSL/dslogic.c
    - DSView/DSView/pv/prop/binding/deviceoptions.cpp
    - DSView/NEWS31
  </read_first>
  <behavior>
    - A request with a supported operation mode, channel mode, sample rate, sample limit, enabled-channel set, threshold, and filter validates to one normalized `ValidatedDeviceOptionRequest`.
    - Invalid combinations fail before runtime apply with exact machine codes rather than free-form classification.
  </behavior>
  <action>Start in RED by expanding the predecessor test target `crates/dsview-core/tests/device_option_validation.rs` with the first requirement-specific cases this task must satisfy: at minimum `buffer_100x16_accepts_100mhz_and_four_channels`, `buffer_400x4_rejects_five_enabled_channels`, `buffer_100x16_rejects_200mhz_samplerate`, and `sample_limit_alignment_can_push_request_over_capacity`. Then implement `DeviceOptionValidationCapabilities::validate_request(&self, request: &DeviceOptionValidationRequest) -> Result<ValidatedDeviceOptionRequest, DeviceOptionValidationError>` in `crates/dsview-core/src/device_option_validation.rs`. Keep the stable `DeviceOptionValidationError` variants and `code()` strings exactly named `operation_mode_unsupported`, `channel_mode_unsupported`, `channel_mode_incompatible`, `sample_rate_missing`, `sample_rate_unsupported`, `sample_limit_missing`, `enabled_channels_empty`, `channel_out_of_range`, `enabled_channels_exceed_mode_limit`, `sample_limit_exceeds_capacity`, `threshold_out_of_range`, `threshold_step_invalid`, `filter_unsupported`, `stop_option_unsupported`, and `stop_option_incompatible`. Validate only against Phase 10 stable IDs and the Phase 11 capability snapshot: resolve the selected operation mode first, require the selected channel mode to belong to that operation mode, require `sample_rate_hz > 0`, require `sample_limit > 0`, require at least one enabled channel, reject channel indexes `>= total_channel_count`, reject enabled-channel counts beyond `max_enabled_channels`, require `supported_sample_rates.contains(&request.sample_rate_hz)`, and reuse the same sample-limit alignment/capacity math as `capture_config.rs` by promoting the existing helper functions there to shared `pub(crate)` helpers instead of duplicating the arithmetic. Export `Discovery::validate_device_option_request(selection_handle, request)` from `crates/dsview-core/src/lib.rs` as the future CLI entrypoint. Per the resolved planning decisions in `11-RESEARCH.md`, reserve threshold step validation for epsilon-tolerant checking and keep stop-option compatibility data-driven with buffer-mode-only compatibility for this milestone.</action>
  <acceptance_criteria>
    - `rg -n "pub enum DeviceOptionValidationError|pub fn code\\(&self\\) -> &'static str|pub fn validate_request" crates/dsview-core/src/device_option_validation.rs` finds the stable error enum, code mapper, and pure validator.
    - `rg -n "buffer_100x16_accepts_100mhz_and_four_channels|buffer_400x4_rejects_five_enabled_channels|sample_limit_alignment_can_push_request_over_capacity" crates/dsview-core/tests/device_option_validation.rs` finds the first RED/GREEN requirement cases in the existing target.
    - `rg -n "operation_mode_unsupported|channel_mode_incompatible|sample_rate_unsupported|sample_limit_exceeds_capacity|threshold_step_invalid|stop_option_incompatible" crates/dsview-core/src/device_option_validation.rs` finds the exact stable validation code strings.
    - `rg -n "pub fn validate_device_option_request" crates/dsview-core/src/lib.rs` shows the selected-device validation entrypoint exists.
    - `cargo test -p dsview-core --test device_option_validation -- --nocapture` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p dsview-core --test device_option_validation -- --nocapture</automated>
  </verify>
  <done>`dsview-core` can validate a unified DSLogic Plus option request against Phase 11 capabilities and return typed, machine-readable failures without doing runtime apply work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Map validation failures to stable CLI codes and keep the `v1.0` baseline intact</name>
  <files>crates/dsview-cli/src/main.rs, crates/dsview-core/src/device_option_validation.rs, crates/dsview-core/src/capture_config.rs</files>
  <read_first>
    - .planning/phases/11-device-option-validation-model/11-RESEARCH.md
    - .planning/phases/11-device-option-validation-model/11-VALIDATION.md
    - crates/dsview-cli/src/main.rs
    - crates/dsview-core/src/device_option_validation.rs
    - crates/dsview-core/src/capture_config.rs
  </read_first>
  <behavior>
    - Existing CLI stable-code assertions expand before capture-path wiring changes, so validation-code regressions fail in RED first.
    - Known pre-acquisition validation failures map through `classify_validation_error()` instead of the generic `runtime_error` fallback.
  </behavior>
  <action>Start in RED by expanding the predecessor CLI assertions already scaffolded in `crates/dsview-cli/src/main.rs`; add direct tests for `classify_validation_error()` that cover at least `sample_rate_unsupported`, `enabled_channels_exceed_mode_limit`, `sample_limit_exceeds_capacity`, `threshold_out_of_range`, `filter_unsupported`, and `stop_option_incompatible`. Then update `crates/dsview-cli/src/main.rs` so `fn classify_validation_error(error: &DeviceOptionValidationError) -> ErrorResponse` uses `error.code()` directly for `ErrorResponse.code`, preserves the human-readable message from `Display`, and leaves `native_error`, `terminal_event`, and `cleanup` as `None`. Update the current `run_capture()` validation failure path so known pre-acquisition failures no longer flow through `classify_runtime_error(&RuntimeError::InvalidArgument(...))`. Use the Phase 11 validation taxonomy for current capture-config failures by adding an adapter from `CaptureConfigError` to the closest `DeviceOptionValidationError` code for the existing fields, while leaving real runtime/bridge failures on the existing `classify_runtime_error()` path. Do not change the JSON/text response shape for other failure classes, and do not add any new CLI flags in this phase.</action>
  <acceptance_criteria>
    - `rg -n "fn classify_validation_error" crates/dsview-cli/src/main.rs` finds the dedicated validation mapper.
    - `rg -n "sample_rate_unsupported_maps_to_stable_validation_error_code|stop_option_incompatible_maps_to_stable_validation_error_code" crates/dsview-cli/src/main.rs` finds the expanded CLI validation-code tests.
    - `rg -n "code: error\\.code\\(\\)" crates/dsview-cli/src/main.rs` shows stable CLI codes come directly from the typed validation enum.
    - `! rg -n "classify_runtime_error\\(&RuntimeError::InvalidArgument\\(error.to_string\\(\\)\\)\\)" crates/dsview-cli/src/main.rs` succeeds, proving the old generic validation mapping path is gone.
    - `cargo test -p dsview-core --test device_option_validation -- --nocapture && cargo test -p dsview-cli --lib stable_validation_error_codes -- --nocapture` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p dsview-core --test device_option_validation -- --nocapture && cargo test -p dsview-cli --lib stable_validation_error_codes -- --nocapture</automated>
  </verify>
  <done>Known validation failures now map to stable CLI codes instead of collapsing into `runtime_error`, while all non-validation CLI error classes keep their existing behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argument layer -> core validator | User-supplied IDs and numeric values must be treated as untrusted until the pure validator resolves them against capabilities |
| Core validator -> runtime apply path | Invalid configurations must be rejected before any native apply or acquisition work can begin |
| Core validation enum -> CLI error response | Stable machine-readable codes must survive translation into CLI JSON/text errors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | T | `crates/dsview-core/src/device_option_validation.rs` | mitigate (blocking) | Allowlist every stable ID against the capability snapshot, reject unsupported combinations before runtime calls, and keep validation pure |
| T-11-02-02 | D | Sample-limit and enabled-channel math | mitigate (blocking) | Reuse shared alignment/capacity helpers from `capture_config.rs` so Phase 11 matches the shipped depth rules instead of drifting |
| T-11-02-03 | R/T | `crates/dsview-cli/src/main.rs` error mapping | mitigate | Add a dedicated `classify_validation_error` path so machine-readable codes remain stable and do not regress to `runtime_error` |
</threat_model>

<verification>
- `cargo test -p dsview-core --lib -- --nocapture`
- `cargo test -p dsview-cli --lib -- --nocapture`
</verification>

<success_criteria>
- The unified Phase 11 request can be validated in pure Rust against selected-device capabilities
- Invalid option combinations fail with stable code strings covering both `VAL-01` and `VAL-02`
- CLI reporting uses the stable validation taxonomy for pre-acquisition validation failures
</success_criteria>

<output>
After completion, create `.planning/phases/11-device-option-validation-model/11-02-SUMMARY.md`
</output>
