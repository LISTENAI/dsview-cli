---
phase: 17-decode-output-and-workflow-reporting
plan: 03
type: execute
wave: 3
depends_on:
  - 17-02
files_modified:
  - crates/dsview-cli/tests/decode_cli.rs
  - crates/dsview-cli/src/lib.rs
  - crates/dsview-cli/src/main.rs
  - crates/dsview-core/tests/decode_execute.rs
autonomous: true
requirements:
  - DEC-06
  - PIPE-01
must_haves:
  truths:
    - "Phase 17 locks the final decode output/reporting contract with end-to-end regression coverage."
    - "The final reporting contract preserves machine readability for both success and failure paths."
    - "Users and automation can rely on a stable decode workflow result without needing GUI tooling."
  artifacts:
    - path: crates/dsview-cli/tests/decode_cli.rs
      provides: "Final CLI contract coverage for decode reporting"
      contains: "decode_run_success_json_report_matches_contract"
    - path: crates/dsview-core/tests/decode_execute.rs
      provides: "Core regression coverage for final report projection semantics"
      contains: "offline_decode_failure_report_keeps_binary_status_with_partial_diagnostics"
    - path: crates/dsview-cli/src/lib.rs
      provides: "Finalized text/JSON report rendering behavior"
      contains: "render_decode_report_text"
  key_links:
    - from: crates/dsview-cli/tests/decode_cli.rs
      to: crates/dsview-cli/src/main.rs
      via: "CLI contract tests lock the final decode workflow behavior end to end"
      pattern: "decode_run_success_json_report_matches_contract|decode_run_failure_json_report_matches_contract"
---

<objective>
Lock the final Phase 17 decode reporting contract end to end with regression coverage and any last-mile report-shape cleanups.

Purpose: The previous Phase 17 plans define and implement the reporting contract. This final plan makes it safe to rely on by locking the success/failure schema, stdout/file alignment, and text/JSON behavior in regression tests.
Output: Final end-to-end decode reporting contract coverage and any last-mile schema cleanup needed to keep the workflow stable.
</objective>

<execution_context>
@/home/seasonyuu/.codex/get-shit-done/workflows/execute-plan.md
@/home/seasonyuu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-decode-output-and-workflow-reporting/17-CONTEXT.md
@.planning/phases/17-decode-output-and-workflow-reporting/17-RESEARCH.md
@.planning/phases/17-decode-output-and-workflow-reporting/17-VALIDATION.md
@.planning/phases/17-decode-output-and-workflow-reporting/17-01-PLAN.md
@.planning/phases/17-decode-output-and-workflow-reporting/17-02-PLAN.md
@CLAUDE.md
@crates/dsview-cli/tests/decode_cli.rs
@crates/dsview-cli/src/main.rs
@crates/dsview-cli/src/lib.rs
@crates/dsview-core/tests/decode_execute.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add final end-to-end decode reporting contract regressions</name>
  <files>crates/dsview-cli/tests/decode_cli.rs, crates/dsview-core/tests/decode_execute.rs</files>
  <read_first>
    - .planning/phases/17-decode-output-and-workflow-reporting/17-CONTEXT.md
    - .planning/phases/17-decode-output-and-workflow-reporting/17-RESEARCH.md
    - .planning/phases/17-decode-output-and-workflow-reporting/17-VALIDATION.md
    - crates/dsview-cli/tests/decode_cli.rs
    - crates/dsview-core/tests/decode_execute.rs
  </read_first>
  <behavior>
    - End-to-end CLI coverage locks the final JSON success schema.
    - End-to-end CLI coverage locks the final JSON failure schema.
    - Core coverage locks the binary-status + partial-diagnostics behavior under the final report model.
  </behavior>
  <action>In `crates/dsview-cli/tests/decode_cli.rs`, add final contract tests named `decode_run_success_json_report_matches_contract`, `decode_run_failure_json_report_matches_contract`, and `decode_run_text_output_is_summary_focused`. In `crates/dsview-core/tests/decode_execute.rs`, add a regression named `offline_decode_failure_report_keeps_binary_status_with_partial_diagnostics`. Make these tests assert the final top-level shape, stable error-code fields, the presence/absence rules for `events`, `partial_events`, and `diagnostics`, and the continued absence of any `partial_success` public state.</action>
  <acceptance_criteria>
    - `rg -n "decode_run_success_json_report_matches_contract|decode_run_failure_json_report_matches_contract|decode_run_text_output_is_summary_focused" crates/dsview-cli/tests/decode_cli.rs` finds the final CLI contract tests.
    - `rg -n "offline_decode_failure_report_keeps_binary_status_with_partial_diagnostics" crates/dsview-core/tests/decode_execute.rs` finds the core reporting regression.
    - `cargo test -p dsview-cli --test decode_cli -- --nocapture` exits 0.
    - `cargo test -p dsview-core --test decode_execute -- --nocapture` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p dsview-cli --test decode_cli -- --nocapture && cargo test -p dsview-core --test decode_execute -- --nocapture</automated>
  </verify>
  <done>The final decode reporting contract is regression-locked for both success and failure paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Perform final report-shape cleanup without changing semantics</name>
  <files>crates/dsview-cli/src/lib.rs, crates/dsview-cli/src/main.rs</files>
  <read_first>
    - .planning/phases/17-decode-output-and-workflow-reporting/17-CONTEXT.md
    - crates/dsview-cli/src/lib.rs
    - crates/dsview-cli/src/main.rs
    - crates/dsview-cli/tests/decode_cli.rs
  </read_first>
  <behavior>
    - Any last-mile field renames or renderer tweaks preserve the locked semantics from Plans 17-01 and 17-02.
    - Text output stays summary-focused.
    - JSON output remains authoritative and machine-readable.
  </behavior>
  <action>If the new contract tests reveal mismatches or awkward field naming, make the minimal cleanup changes needed in `crates/dsview-cli/src/lib.rs` and `crates/dsview-cli/src/main.rs` so the final decode reporting schema is coherent and automation-friendly. Do not broaden scope, add new command behaviors, or change the already-locked semantics around flat events, binary run status, or stdout/`--output` behavior.</action>
  <acceptance_criteria>
    - The final JSON schema remains `run + flat events` on success and `run + error (+ optional partial diagnostics)` on failure.
    - Text output remains concise and does not dump the full event list by default.
    - `! rg -n "partial_success" crates/dsview-cli/src/lib.rs crates/dsview-cli/src/main.rs` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p dsview-cli --lib -- --nocapture</automated>
  </verify>
  <done>The final CLI report shape is coherent, stable, and aligned with the locked Phase 17 semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| final public schema -> automation consumers | schema drift or ambiguity would break downstream tooling |
| text renderer -> human operators | text output must stay concise while still matching the underlying machine-readable truth |
| failure contract -> diagnostics exposure | final cleanup must not accidentally weaken binary status semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03-01 | T | final public schema | mitigate | lock success and failure contracts end to end with CLI regressions |
| T-17-03-02 | I | text-vs-json rendering | mitigate | keep JSON authoritative and test text as a summary renderer only |
| T-17-03-03 | R | final cleanup drift | mitigate | restrict last-mile cleanup to naming/rendering only and forbid semantic broadening |
</threat_model>

<verification>
- `cargo test -p dsview-cli --test decode_cli -- --nocapture`
- `cargo test -p dsview-core --test decode_execute -- --nocapture`
- `cargo test -p dsview-cli --lib -- --nocapture`
</verification>

<success_criteria>
- The final decode output/reporting contract is regression-locked end to end
- Success and failure JSON schemas are stable and machine-readable
- Text output remains a concise human renderer over the same canonical result model
</success_criteria>

<output>
After completion, create `.planning/phases/17-decode-output-and-workflow-reporting/17-03-SUMMARY.md`
</output>
