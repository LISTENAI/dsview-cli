---
phase: 17-decode-output-and-workflow-reporting
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - crates/dsview-cli/src/main.rs
  - crates/dsview-cli/src/lib.rs
  - crates/dsview-core/src/lib.rs
  - crates/dsview-core/tests/decode_execute.rs
  - crates/dsview-cli/tests/decode_cli.rs
autonomous: true
requirements:
  - DEC-06
  - PIPE-01
must_haves:
  truths:
    - "Failures use stable, fine-grained error codes while remaining separate from success reporting."
    - "Partial diagnostics may be written or emitted on failure, but they do not change the run status from `failure`."
    - "`--output` persists the same canonical result document shape that stdout uses by default."
  artifacts:
    - path: crates/dsview-cli/src/main.rs
      provides: "Final decode error/reporting behavior including optional output-file handling"
      contains: "classify_decode_run_error"
    - path: crates/dsview-cli/src/lib.rs
      provides: "Artifact-writing aware report serialization helpers"
      contains: "write_decode_report"
    - path: crates/dsview-cli/tests/decode_cli.rs
      provides: "Coverage for failure reports, partial diagnostics, and optional output writing"
      contains: "decode_run_failure_can_emit_partial_diagnostics"
  key_links:
    - from: crates/dsview-cli/src/main.rs
      to: crates/dsview-cli/src/lib.rs
      via: "CLI execution routes stdout and optional artifact writing through the same canonical report serializer"
      pattern: "--output|write_decode_report"
---

<objective>
Finalize failure reporting semantics, partial diagnostics exposure, and optional artifact writing for decode runs.

Purpose: Phase 17 must turn the final reporting schema into a complete user-facing contract, including how failures are represented and how results can optionally be persisted to disk.
Output: Stable failure/output behavior for `decode run`, including optional `--output` file writing.
</objective>

<execution_context>
@/home/seasonyuu/.codex/get-shit-done/workflows/execute-plan.md
@/home/seasonyuu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-decode-output-and-workflow-reporting/17-CONTEXT.md
@.planning/phases/17-decode-output-and-workflow-reporting/17-RESEARCH.md
@.planning/phases/17-decode-output-and-workflow-reporting/17-VALIDATION.md
@.planning/phases/17-decode-output-and-workflow-reporting/17-01-PLAN.md
@.planning/phases/16-offline-decode-execution/16-VERIFICATION.md
@CLAUDE.md
@crates/dsview-cli/src/main.rs
@crates/dsview-cli/src/lib.rs
@crates/dsview-core/src/lib.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Finalize decode failure reporting and partial diagnostics semantics</name>
  <files>crates/dsview-cli/src/main.rs, crates/dsview-core/src/lib.rs, crates/dsview-core/tests/decode_execute.rs</files>
  <read_first>
    - .planning/phases/17-decode-output-and-workflow-reporting/17-CONTEXT.md
    - .planning/phases/17-decode-output-and-workflow-reporting/17-RESEARCH.md
    - crates/dsview-cli/src/main.rs
    - crates/dsview-core/src/lib.rs
    - crates/dsview-core/tests/decode_execute.rs
  </read_first>
  <behavior>
    - Failure reports use stable, fine-grained error codes.
    - Partial diagnostics and partial events can be surfaced externally on failure.
    - Exposing diagnostics never changes the run status away from `failure`.
  </behavior>
  <action>In `crates/dsview-core/src/lib.rs`, finalize any supplementary failure-report structs needed to carry partial annotations and diagnostics out to the CLI. In `crates/dsview-cli/src/main.rs`, update `classify_decode_run_error` and related success/failure wiring so runtime, input, config, and artifact-write failures use stable and distinct error codes while optionally carrying partial diagnostics/report payload fields. Keep the top-level run status binary. Add tests in `crates/dsview-core/tests/decode_execute.rs` or CLI tests that prove partial diagnostics are surfaced on failure but never create a `partial_success` state.</action>
  <acceptance_criteria>
    - `rg -n "classify_decode_run_error|decode_session_send_failed|decode_session_end_failed|decode_output_write_failed" crates/dsview-cli/src/main.rs` finds the final fine-grained failure mapping.
    - `rg -n "partial_events|diagnostics" crates/dsview-core/src/lib.rs crates/dsview-cli/src/main.rs` finds explicit supplementary failure fields.
    - `! rg -n "partial_success" crates/dsview-core/src/lib.rs crates/dsview-cli/src/main.rs` succeeds, proving the phase does not introduce a public degraded-success state.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p dsview-core --test decode_execute -- --nocapture && cargo test -p dsview-cli --bin dsview-cli -- --nocapture</automated>
  </verify>
  <done>Failure reports now expose stable codes plus optional partial diagnostics without weakening the binary success/failure model.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add optional decode report artifact writing and lock stdout/file contract behavior</name>
  <files>crates/dsview-cli/src/main.rs, crates/dsview-cli/src/lib.rs, crates/dsview-cli/tests/decode_cli.rs</files>
  <read_first>
    - .planning/phases/17-decode-output-and-workflow-reporting/17-CONTEXT.md
    - .planning/phases/17-decode-output-and-workflow-reporting/17-RESEARCH.md
    - crates/dsview-cli/src/main.rs
    - crates/dsview-cli/src/lib.rs
    - crates/dsview-cli/tests/decode_cli.rs
  </read_first>
  <behavior>
    - stdout remains the default decode result path.
    - `--output` writes the canonical result document to disk.
    - stdout and file output stay structurally aligned.
  </behavior>
  <action>In `crates/dsview-cli/src/main.rs`, add an optional `--output <PATH>` for `decode run`. Implement file writing in `crates/dsview-cli/src/lib.rs` or a nearby helper so the same canonical JSON result document written to stdout can also be persisted to disk. Decide and implement whether failures with partial diagnostics also write an artifact when `--output` is present, but keep that behavior explicit and test-covered. In `crates/dsview-cli/tests/decode_cli.rs`, add regressions named `decode_run_success_writes_output_artifact`, `decode_run_failure_writes_failure_report_when_output_requested`, and `decode_run_stdout_and_file_output_share_the_same_schema`.</action>
  <acceptance_criteria>
    - `rg -n "--output|output: Option<PathBuf>|write_decode_report" crates/dsview-cli/src/main.rs crates/dsview-cli/src/lib.rs` finds the artifact-writing path.
    - `rg -n "decode_run_success_writes_output_artifact|decode_run_failure_writes_failure_report_when_output_requested|decode_run_stdout_and_file_output_share_the_same_schema" crates/dsview-cli/tests/decode_cli.rs` finds the new artifact/output regressions.
    - `cargo test -p dsview-cli --test decode_cli -- --nocapture` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p dsview-cli --test decode_cli -- --nocapture</automated>
  </verify>
  <done>`decode run` now supports optional artifact writing without abandoning stdout as the default interface, and the stdout/file schema is regression-locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| core report -> CLI surface | the final public schema must remain stable and machine-readable |
| failure result -> partial diagnostics | partial diagnostics must not confuse automation into treating failures as success |
| stdout -> file artifact | stdout and persisted reports must not diverge semantically |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02-01 | T | public failure contract | mitigate | use stable fine-grained error codes and keep partial diagnostics supplementary |
| T-17-02-02 | I | stdout/file divergence | mitigate | write the same canonical report schema to both stdout and optional artifacts |
| T-17-02-03 | R | partial diagnostics semantics | mitigate | forbid `partial_success` and keep top-level run status binary |
</threat_model>

<verification>
- `cargo test -p dsview-core --test decode_execute -- --nocapture`
- `cargo test -p dsview-cli --bin dsview-cli -- --nocapture`
- `cargo test -p dsview-cli --test decode_cli -- --nocapture`
</verification>

<success_criteria>
- Failure reporting is stable, fine-grained, and still binary in status semantics
- Partial diagnostics can be exposed on failure without changing the top-level run state
- Optional artifact writing is available and structurally aligned with stdout JSON
</success_criteria>

<output>
After completion, create `.planning/phases/17-decode-output-and-workflow-reporting/17-02-SUMMARY.md`
</output>
