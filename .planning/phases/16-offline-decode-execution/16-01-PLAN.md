---
phase: 16-offline-decode-execution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/dsview-sys/wrapper.h
  - crates/dsview-sys/bridge_runtime.c
  - crates/dsview-sys/src/lib.rs
  - crates/dsview-sys/tests/boundary.rs
  - crates/dsview-core/src/lib.rs
autonomous: true
requirements:
  - DEC-05
must_haves:
  truths:
    - "Phase 16 defines a canonical raw logic input contract for offline decode instead of treating VCD as the primary execution input."
    - "The sys boundary exposes the minimal session/start/send/end primitives needed for offline decode execution over the existing decode runtime."
    - "Sample input shape is explicit enough to validate split-logic versus cross-logic payloads before execution begins."
  artifacts:
    - path: crates/dsview-core/src/lib.rs
      provides: "Offline decode input contract and validated runtime-execution request types"
      contains: "pub struct OfflineDecodeInput"
    - path: crates/dsview-sys/src/lib.rs
      provides: "Safe Rust wrappers for decode session lifecycle and chunked sample sending"
      contains: "pub fn session_send_logic_chunk"
    - path: crates/dsview-sys/tests/boundary.rs
      provides: "Boundary coverage for offline decode input validation and session primitive wiring"
      contains: "offline_decode_rejects_misaligned_logic_packet_lengths"
  key_links:
    - from: crates/dsview-core/src/lib.rs
      to: crates/dsview-sys/src/lib.rs
      via: "offline decode input contract feeds the new decode session/send wrappers without changing canonical config ids"
      pattern: "OfflineDecodeInput|session_send_logic_chunk"
---

<objective>
Define the offline decode input contract and expose the raw decode session primitives needed to execute chunked offline decode runs.

Purpose: Phase 16 must establish one canonical execution input model over raw logic artifacts and a sys-layer session bridge that can start decoder stacks, inject samplerate metadata, send sample chunks with absolute numbering, and finalize the session.
Output: Typed offline decode input structs in core plus safe session/start/send/end wrappers in `dsview-sys`.
</objective>

<execution_context>
@/home/seasonyuu/.codex/get-shit-done/workflows/execute-plan.md
@/home/seasonyuu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-offline-decode-execution/16-CONTEXT.md
@.planning/phases/16-offline-decode-execution/16-RESEARCH.md
@.planning/phases/16-offline-decode-execution/16-VALIDATION.md
@.planning/phases/15-decode-config-model-and-validation/15-CONTEXT.md
@.planning/phases/15-decode-config-model-and-validation/15-VERIFICATION.md
@CLAUDE.md
@crates/dsview-sys/wrapper.h
@crates/dsview-sys/bridge_runtime.c
@crates/dsview-sys/src/lib.rs
@crates/dsview-sys/tests/boundary.rs
@crates/dsview-core/src/lib.rs
@DSView/libsigrokdecode4DSL/libsigrokdecode.h
@DSView/libsigrokdecode4DSL/session.c
@DSView/libsigrokdecode4DSL/instance.c
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add raw decode session lifecycle and chunked send wrappers in dsview-sys</name>
  <files>crates/dsview-sys/wrapper.h, crates/dsview-sys/bridge_runtime.c, crates/dsview-sys/src/lib.rs, crates/dsview-sys/tests/boundary.rs</files>
  <read_first>
    - .planning/phases/16-offline-decode-execution/16-CONTEXT.md
    - .planning/phases/16-offline-decode-execution/16-RESEARCH.md
    - crates/dsview-sys/wrapper.h
    - crates/dsview-sys/bridge_runtime.c
    - crates/dsview-sys/src/lib.rs
    - crates/dsview-sys/tests/boundary.rs
    - DSView/libsigrokdecode4DSL/libsigrokdecode.h
    - DSView/libsigrokdecode4DSL/session.c
    - DSView/libsigrokdecode4DSL/instance.c
  </read_first>
  <behavior>
    - The sys layer exposes decode session create/start/send/end/destroy operations over the existing decode runtime.
    - Sample chunks are sent with explicit absolute start/end sample numbers.
    - The bridge can distinguish invalid input shape from runtime/session failures.
  </behavior>
  <action>In `crates/dsview-sys/wrapper.h`, add raw decode execution structs and function declarations for session creation, samplerate metadata injection, root-plus-stack decoder instantiation, chunked sample sending, session finalization, and cleanup. In `crates/dsview-sys/bridge_runtime.c`, wire those functions to the upstream `srd_session_new`, `srd_session_metadata_set`, `srd_inst_new`, `srd_inst_channel_set_all`, `srd_inst_stack`, `srd_session_start`, `srd_session_send`, `srd_session_end`, and `srd_session_destroy` APIs. Implement only the minimal linear-stack execution bridge required by Phase 16: the root decoder binds logic channels, downstream decoders are stacked in order, and each send uses absolute sample numbering. In `crates/dsview-sys/src/lib.rs`, add safe wrappers such as `DecodeExecutionSession` and `session_send_logic_chunk` that own error conversion and reject empty/misaligned packet shapes before reaching C. In `crates/dsview-sys/tests/boundary.rs`, add regressions named `offline_decode_rejects_empty_sample_bytes`, `offline_decode_rejects_misaligned_logic_packet_lengths`, and `offline_decode_session_wrappers_require_absolute_sample_progression`.</action>
  <acceptance_criteria>
    - `rg -n "srd_session_new|srd_session_send|srd_session_end|srd_inst_stack" crates/dsview-sys/bridge_runtime.c` finds the new raw bridge wiring for offline decode execution.
    - `rg -n "struct DecodeExecutionSession|pub fn session_send_logic_chunk" crates/dsview-sys/src/lib.rs` finds the new safe execution wrappers.
    - `rg -n "offline_decode_rejects_empty_sample_bytes|offline_decode_rejects_misaligned_logic_packet_lengths|offline_decode_session_wrappers_require_absolute_sample_progression" crates/dsview-sys/tests/boundary.rs` finds the boundary regressions.
    - `! rg -n "VCD" crates/dsview-sys/src/lib.rs` succeeds for the new execution path, proving the canonical runtime input is not modeled as VCD text.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p dsview-sys --test boundary -- --nocapture</automated>
  </verify>
  <done>The sys crate can now start a decode session, send raw logic chunks with absolute numbering, and end the session cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define the canonical offline decode input contract in core</name>
  <files>crates/dsview-core/src/lib.rs</files>
  <read_first>
    - .planning/phases/16-offline-decode-execution/16-CONTEXT.md
    - .planning/phases/16-offline-decode-execution/16-RESEARCH.md
    - crates/dsview-core/src/lib.rs
    - crates/dsview-sys/src/lib.rs
  </read_first>
  <behavior>
    - Core defines one canonical raw offline decode input contract.
    - The contract distinguishes split-logic and cross-logic payload shapes and preserves packet-boundary metadata when available.
    - The resulting request type is ready to pair with `ValidatedDecodeConfig` in later execution code.
  </behavior>
  <action>In `crates/dsview-core/src/lib.rs`, add typed offline decode input types such as `OfflineDecodeInput`, `OfflineDecodeDataFormat`, and any supporting request structs needed to pair that input with `ValidatedDecodeConfig`. Model the canonical payload as raw sample bytes plus explicit samplerate and format metadata, with optional `logic_packet_lengths` when the input originated from packetized data. Include helper methods that compute the sample count and validate basic shape invariants before the sys bridge is called. Keep these types execution-focused and separate from Phase 15 config parsing.</action>
  <acceptance_criteria>
    - `rg -n "pub struct OfflineDecodeInput|pub enum OfflineDecodeDataFormat" crates/dsview-core/src/lib.rs` finds the new core execution-input types.
    - `rg -n "logic_packet_lengths|sample_bytes|samplerate_hz" crates/dsview-core/src/lib.rs` shows the canonical raw input fields are explicit.
    - `! rg -n "Vcd|vcd" crates/dsview-core/src/lib.rs` succeeds for the new input contract, proving the canonical model is raw logic data rather than VCD text.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p dsview-core --lib -- --nocapture</automated>
  </verify>
  <done>Core now owns the raw offline decode input contract that later execution plans can run against.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw artifact bytes -> sys session bridge | malformed sample layout or packet boundaries must fail before decode runtime state is corrupted |
| core offline input contract -> sys send loop | absolute sample numbering must be preserved across all chunks |
| validated config -> runtime stack construction | only the root decoder may bind logic channels; stacked decoders must be linked linearly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01-01 | T | sample-feeding contract | mitigate | validate raw shape in Rust before sending into the C session bridge |
| T-16-01-02 | I | absolute sample numbering | mitigate | maintain explicit absolute cursors and test chunk continuity failures |
| T-16-01-03 | D | stack/session bridge | mitigate | build only strict linear stacks with root-only channel binding and fail on invalid runtime setup |
</threat_model>

<verification>
- `cargo test -p dsview-sys --test boundary -- --nocapture`
- `cargo test -p dsview-core --lib -- --nocapture`
</verification>

<success_criteria>
- The project has a canonical raw offline decode input contract
- The sys layer can open and drive decode sessions with absolute-sample chunking
- The execution bridge does not depend on VCD text as the primary decode input
</success_criteria>

<output>
After completion, create `.planning/phases/16-offline-decode-execution/16-01-SUMMARY.md`
</output>
