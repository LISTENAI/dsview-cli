---
phase: 16-offline-decode-execution
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - crates/dsview-core/src/lib.rs
  - crates/dsview-core/tests/decode_execute.rs
  - crates/dsview-sys/tests/boundary.rs
autonomous: true
requirements:
  - DEC-05
  - DEC-07
must_haves:
  truths:
    - "Phase 16 executes validated decode configs strictly linearly: root decoder consumes logic samples and each stacked decoder consumes the previous decoder's output."
    - "Chunked execution preserves absolute sample numbering regardless of packet-aware or fixed-size chunking strategy."
    - "Critical runtime failures fail the entire decode run from the user-facing workflow perspective."
  artifacts:
    - path: crates/dsview-core/src/lib.rs
      provides: "Offline decode executor and internal execution result type"
      contains: "pub fn run_offline_decode"
    - path: crates/dsview-core/tests/decode_execute.rs
      provides: "Execution coverage for chunking, stack orchestration, and failure behavior"
      contains: "offline_decode_uses_absolute_sample_cursor_across_chunks"
    - path: crates/dsview-sys/tests/boundary.rs
      provides: "Low-level session behavior coverage for stacked runtime forwarding"
      contains: "stacked_decoder_python_output_flows_linearly"
  key_links:
    - from: crates/dsview-core/src/lib.rs
      to: crates/dsview-sys/src/lib.rs
      via: "core execution orchestration drives the sys session bridge with validated config and chunked offline input"
      pattern: "run_offline_decode|session_send_logic_chunk"
---

<objective>
Implement the core offline decode executor that consumes validated configs plus raw input artifacts, builds the linear runtime stack, and runs chunked decode execution with strict failure semantics.

Purpose: Once the input contract and session bridge exist, Phase 16 must actually execute offline decode runs. This plan should make execution deterministic and keep failure semantics binary while retaining internal partial state only for diagnostics.
Output: A core offline decode executor with stack orchestration, chunking policy selection, and execution-time failure handling.
</objective>

<execution_context>
@/home/seasonyuu/.codex/get-shit-done/workflows/execute-plan.md
@/home/seasonyuu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-offline-decode-execution/16-CONTEXT.md
@.planning/phases/16-offline-decode-execution/16-RESEARCH.md
@.planning/phases/16-offline-decode-execution/16-VALIDATION.md
@.planning/phases/16-offline-decode-execution/16-01-PLAN.md
@.planning/phases/15-decode-config-model-and-validation/15-VERIFICATION.md
@CLAUDE.md
@crates/dsview-core/src/lib.rs
@crates/dsview-sys/src/lib.rs
@crates/dsview-sys/tests/boundary.rs
@DSView/libsigrokdecode4DSL/type_decoder.c
@DSView/libsigrokdecode4DSL/instance.c
@DSView/DSView/pv/data/decoderstack.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement core offline decode orchestration with strict linear stack semantics</name>
  <files>crates/dsview-core/src/lib.rs, crates/dsview-core/tests/decode_execute.rs</files>
  <read_first>
    - .planning/phases/16-offline-decode-execution/16-CONTEXT.md
    - .planning/phases/16-offline-decode-execution/16-RESEARCH.md
    - .planning/phases/15-decode-config-model-and-validation/15-VERIFICATION.md
    - crates/dsview-core/src/lib.rs
    - crates/dsview-sys/src/lib.rs
  </read_first>
  <behavior>
    - The core executor consumes `ValidatedDecodeConfig` plus `OfflineDecodeInput` directly.
    - Root decoder channel bindings are applied once; stacked decoders are linked linearly without direct logic-channel binding.
    - Packet-aware execution uses packet boundaries when present; otherwise fixed-size chunking is used.
  </behavior>
  <action>In `crates/dsview-core/src/lib.rs`, add an offline decode executor such as `run_offline_decode()` that accepts a `ValidatedDecodeConfig`, an `OfflineDecodeInput`, and a loaded decode runtime/session handle. Build the runtime stack strictly in config order: instantiate the root decoder with logic channel bindings, instantiate stacked decoders with options only, and link each layer linearly. Implement one absolute sample cursor and use it across all sends; when `logic_packet_lengths` exists, chunk by packet, otherwise use a fixed chunking helper. Add a typed internal execution result that can hold collected annotation events and internal partial diagnostics without exposing `partial_success` publicly. In `crates/dsview-core/tests/decode_execute.rs`, add tests named `offline_decode_uses_absolute_sample_cursor_across_chunks`, `offline_decode_prefers_packet_boundaries_when_available`, and `offline_decode_root_only_binds_logic_channels`.</action>
  <acceptance_criteria>
    - `rg -n "pub fn run_offline_decode|struct OfflineDecodeResult" crates/dsview-core/src/lib.rs` finds the new execution entrypoint and internal result type.
    - `rg -n "offline_decode_uses_absolute_sample_cursor_across_chunks|offline_decode_prefers_packet_boundaries_when_available|offline_decode_root_only_binds_logic_channels" crates/dsview-core/tests/decode_execute.rs` finds the execution regressions.
    - `! rg -n "partial_success" crates/dsview-core/src/lib.rs` succeeds, proving the phase does not introduce a public degraded-success state.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p dsview-core --test decode_execute -- --nocapture</automated>
  </verify>
  <done>Core can now execute validated offline decode runs with deterministic chunking and strict linear stack semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Propagate stacked decoder output and fail hard on runtime send/end errors</name>
  <files>crates/dsview-sys/tests/boundary.rs, crates/dsview-core/src/lib.rs, crates/dsview-core/tests/decode_execute.rs</files>
  <read_first>
    - .planning/phases/16-offline-decode-execution/16-CONTEXT.md
    - .planning/phases/16-offline-decode-execution/16-RESEARCH.md
    - crates/dsview-sys/tests/boundary.rs
    - crates/dsview-core/src/lib.rs
    - crates/dsview-core/tests/decode_execute.rs
    - DSView/libsigrokdecode4DSL/type_decoder.c
    - DSView/libsigrokdecode4DSL/session.c
  </read_first>
  <behavior>
    - Annotation capture follows the upstream callback path and stacked decoder forwarding behavior.
    - Critical send/end/runtime failures fail the whole decode run.
    - Internal partial annotations may be retained for diagnostics, but the returned run status remains binary.
  </behavior>
  <action>In `crates/dsview-sys/tests/boundary.rs`, add a targeted regression named `stacked_decoder_python_output_flows_linearly` or an equivalent session-level test that proves a real stacked decoder chain can be constructed using the Phase 14/16 bridge shape and that downstream decoders consume upstream `OUTPUT_PYTHON` data in order. In `crates/dsview-core/src/lib.rs`, ensure send-loop and finalization failures propagate as hard execution failures, while any collected partial annotations remain internal fields on the result/error structures only. In `crates/dsview-core/tests/decode_execute.rs`, add tests named `offline_decode_fails_when_session_send_fails`, `offline_decode_fails_when_session_end_fails`, and `offline_decode_retains_partial_annotations_for_diagnostics_only`.</action>
  <acceptance_criteria>
    - `rg -n "stacked_decoder_python_output_flows_linearly" crates/dsview-sys/tests/boundary.rs` finds the stacked-runtime forwarding coverage.
    - `rg -n "offline_decode_fails_when_session_send_fails|offline_decode_fails_when_session_end_fails|offline_decode_retains_partial_annotations_for_diagnostics_only" crates/dsview-core/tests/decode_execute.rs` finds the failure-behavior regressions.
    - `! rg -n "partial_success" crates/dsview-core/tests/decode_execute.rs` succeeds, proving diagnostics retention did not become a public execution status.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p dsview-sys --test boundary -- --nocapture && cargo test -p dsview-core --test decode_execute -- --nocapture</automated>
  </verify>
  <done>The offline decode executor now respects stacked runtime forwarding and fails the whole run when send/end operations fail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validated config -> runtime stack construction | stack order and binding rules must not drift from the validated linear model |
| chunking policy -> decode runtime | packet/chunk boundaries must preserve absolute sample numbering |
| runtime callbacks/errors -> user-facing run state | internal partial state must not be confused with public success semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02-01 | T | stack construction | mitigate | instantiate and link decoders strictly in validated order with root-only logic channel binding |
| T-16-02-02 | D | send/end failure handling | mitigate | fail hard on session send/end failures and cover those paths with regression tests |
| T-16-02-03 | I | partial diagnostics retention | mitigate | retain internal annotation state for debugging only and avoid a public partial-success contract |
</threat_model>

<verification>
- `cargo test -p dsview-sys --test boundary -- --nocapture`
- `cargo test -p dsview-core --test decode_execute -- --nocapture`
</verification>

<success_criteria>
- The project can execute validated offline decode runs over chunked raw input
- Stacked decoder execution is strictly linear and grounded in upstream `OUTPUT_PYTHON` semantics
- Critical runtime failures fail the whole run while keeping partial diagnostics internal only
</success_criteria>

<output>
After completion, create `.planning/phases/16-offline-decode-execution/16-02-SUMMARY.md`
</output>
