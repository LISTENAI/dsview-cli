---
phase: 16-offline-decode-execution
plan: 03
type: execute
wave: 3
depends_on:
  - 16-02
files_modified:
  - crates/dsview-cli/src/main.rs
  - crates/dsview-cli/src/lib.rs
  - crates/dsview-cli/tests/decode_cli.rs
  - crates/dsview-core/src/lib.rs
  - crates/dsview-core/tests/decode_execute.rs
autonomous: true
requirements:
  - DEC-05
  - DEC-07
must_haves:
  truths:
    - "Users can run offline decode from the CLI against saved raw logic artifacts without requiring GUI components."
    - "The CLI execution path uses the validated config plus canonical raw input contract, not VCD text as the primary execution source."
    - "Execution success/failure remains binary in this phase even if internal diagnostics retain partial annotations."
  artifacts:
    - path: crates/dsview-cli/src/main.rs
      provides: "`decode run` CLI command wiring for offline execution"
      contains: "Run(DecodeRunArgs)"
    - path: crates/dsview-cli/src/lib.rs
      provides: "Phase 16 execution success rendering over the internal offline decode result"
      contains: "render_decode_run_text"
    - path: crates/dsview-cli/tests/decode_cli.rs
      provides: "CLI execution coverage for successful and failing offline decode runs"
      contains: "decode_run_executes_valid_offline_decode_config"
  key_links:
    - from: crates/dsview-cli/src/main.rs
      to: crates/dsview-core/src/lib.rs
      via: "CLI offline execution delegates to the core decode executor using validated config plus raw artifact input"
      pattern: "decode run|run_offline_decode"
---

<objective>
Expose Phase 16's offline decode execution through the CLI and lock its basic success/failure contract.

Purpose: Phase 16 must make offline decode runnable from the CLI. This should work against saved raw logic artifacts and validated configs while leaving final output/reporting polish for Phase 17.
Output: `decode run` CLI wiring plus execution regressions for representative success and failure scenarios.
</objective>

<execution_context>
@/home/seasonyuu/.codex/get-shit-done/workflows/execute-plan.md
@/home/seasonyuu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-offline-decode-execution/16-CONTEXT.md
@.planning/phases/16-offline-decode-execution/16-RESEARCH.md
@.planning/phases/16-offline-decode-execution/16-VALIDATION.md
@.planning/phases/16-offline-decode-execution/16-02-PLAN.md
@CLAUDE.md
@crates/dsview-cli/src/main.rs
@crates/dsview-cli/src/lib.rs
@crates/dsview-core/src/lib.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the offline decode execution CLI command and minimal result rendering</name>
  <files>crates/dsview-cli/src/main.rs, crates/dsview-cli/src/lib.rs, crates/dsview-core/src/lib.rs</files>
  <read_first>
    - .planning/phases/16-offline-decode-execution/16-CONTEXT.md
    - .planning/phases/16-offline-decode-execution/16-RESEARCH.md
    - crates/dsview-cli/src/main.rs
    - crates/dsview-cli/src/lib.rs
    - crates/dsview-core/src/lib.rs
  </read_first>
  <behavior>
    - The CLI exposes an offline `decode run` command that accepts a validated config path and a raw logic input artifact path.
    - The execution command uses raw logic artifacts as the primary source of truth, not VCD text.
    - Success rendering is sufficient for Phase 16 execution validation without locking the final Phase 17 reporting contract.
  </behavior>
  <action>In `crates/dsview-cli/src/main.rs`, add a `Run` subcommand under `DecodeCommand` that accepts a config path and the raw offline input path(s) needed by the new execution contract, along with the existing decode runtime / decoder-dir override flags and `--format`. Wire it to the new core offline decode executor so the command validates config + input contract, then runs the decode session offline. In `crates/dsview-cli/src/lib.rs`, add minimal success rendering for Phase 16 execution, including enough information to show the root decoder id, stack depth, sample count or chunk count, and a coarse annotation/result summary, while avoiding any promise that this is the final reporting shape. Keep error rendering on the existing stable CLI error path.</action>
  <acceptance_criteria>
    - `rg -n "Run\(DecodeRunArgs\)|struct DecodeRunArgs" crates/dsview-cli/src/main.rs` finds the new CLI execution command wiring.
    - `rg -n "render_decode_run_text|DecodeRunResponse" crates/dsview-cli/src/lib.rs` finds the new execution response/rendering helpers.
    - `! rg -n "VCD" crates/dsview-cli/src/main.rs crates/dsview-cli/src/lib.rs` succeeds for the new execution input path, proving the command is not modeled around VCD text.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p dsview-cli --lib -- --nocapture</automated>
  </verify>
  <done>The CLI can now launch offline decode execution over raw logic artifacts and produce a basic execution result summary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock offline decode run success and failure behavior with CLI regressions</name>
  <files>crates/dsview-cli/tests/decode_cli.rs, crates/dsview-core/tests/decode_execute.rs, crates/dsview-cli/src/main.rs</files>
  <read_first>
    - .planning/phases/16-offline-decode-execution/16-CONTEXT.md
    - .planning/phases/16-offline-decode-execution/16-RESEARCH.md
    - crates/dsview-cli/tests/decode_cli.rs
    - crates/dsview-core/tests/decode_execute.rs
    - crates/dsview-cli/src/main.rs
  </read_first>
  <behavior>
    - Spawned CLI tests cover a successful offline decode run and representative runtime/input failure cases.
    - Invalid artifact shapes fail with stable machine-readable errors.
    - Execution failures remain binary from the CLI perspective.
  </behavior>
  <action>In `crates/dsview-cli/tests/decode_cli.rs`, add CLI contract tests named `decode_run_executes_valid_offline_decode_config`, `decode_run_rejects_misaligned_logic_packet_lengths`, and `decode_run_fails_when_runtime_execution_errors`. Use temp raw input fixtures and the existing debug-only seam pattern where necessary so the tests can execute deterministically without a packaged runtime requirement. In `crates/dsview-core/tests/decode_execute.rs`, add any fixture helpers needed so the CLI tests exercise the real offline executor rather than duplicated mock logic. Ensure the CLI error code path remains stable for invalid artifact shapes and runtime execution failures.</action>
  <acceptance_criteria>
    - `rg -n "decode_run_executes_valid_offline_decode_config|decode_run_rejects_misaligned_logic_packet_lengths|decode_run_fails_when_runtime_execution_errors" crates/dsview-cli/tests/decode_cli.rs` finds the offline execution CLI regressions.
    - `cargo test -p dsview-cli --test decode_cli -- --nocapture` exits 0.
    - `! rg -n "partial_success" crates/dsview-cli/tests/decode_cli.rs crates/dsview-cli/src/main.rs` succeeds, proving the CLI contract remains binary in Phase 16.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p dsview-cli --test decode_cli -- --nocapture && cargo test -p dsview-core --test decode_execute -- --nocapture</automated>
  </verify>
  <done>The CLI offline execution path is regression-locked for representative valid and invalid runs without introducing public partial-success semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-facing CLI -> offline execution engine | CLI inputs must map cleanly to validated config + raw artifact execution without hidden VCD/text conversion assumptions |
| execution result -> user-facing contract | Phase 16 must not expose internal partial diagnostics as a public degraded-success status |
| runtime failure paths -> CLI errors | malformed artifact shapes and runtime execution errors must produce stable, machine-readable failures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03-01 | T | CLI execution input path | mitigate | wire `decode run` directly to validated config + raw artifact types rather than ad-hoc text parsing |
| T-16-03-02 | I | execution result rendering | mitigate | keep Phase 16 output minimal and avoid promising the final Phase 17 reporting schema |
| T-16-03-03 | R | runtime failure surfacing | mitigate | lock binary success/failure semantics and stable CLI error coverage with spawned tests |
</threat_model>

<verification>
- `cargo test -p dsview-cli --lib -- --nocapture`
- `cargo test -p dsview-cli --test decode_cli -- --nocapture`
- `cargo test -p dsview-core --test decode_execute -- --nocapture`
</verification>

<success_criteria>
- Users can launch offline decode runs from the CLI against raw logic artifacts
- Representative success and failure cases are covered by CLI and core regression tests
- Phase 16 keeps execution semantics binary and leaves final reporting polish for Phase 17
</success_criteria>

<output>
After completion, create `.planning/phases/16-offline-decode-execution/16-03-SUMMARY.md`
</output>
