---
phase: 01-native-integration-foundation
plan: 01
title: Create Rust workspace and crate boundaries
subsystem: foundation
tags: [rust, workspace, cli, core, ffi]
wave: 1
autonomous: true
depends_on: []
requirements: [DEV-01]
requirements_addressed: [DEV-01]
files_modified:
  - Cargo.toml
  - crates/dsview-cli/Cargo.toml
  - crates/dsview-cli/src/main.rs
  - crates/dsview-core/Cargo.toml
  - crates/dsview-core/src/lib.rs
  - crates/dsview-sys/Cargo.toml
  - crates/dsview-sys/src/lib.rs
must_haves:
  truths:
    - Rust workspace exists with separate CLI, core, and native crates.
    - Unsafe/native ownership is isolated to the sys crate boundary.
    - The workspace builds without requiring DSView GUI code in Rust crates.
  artifacts:
    - Cargo.toml
    - crates/dsview-cli/Cargo.toml
    - crates/dsview-core/Cargo.toml
    - crates/dsview-sys/Cargo.toml
    - crates/dsview-cli/src/main.rs
    - crates/dsview-core/src/lib.rs
    - crates/dsview-sys/src/lib.rs
  key_links:
    - from: crates/dsview-cli
      to: crates/dsview-core
      via: Cargo workspace dependency
    - from: crates/dsview-core
      to: crates/dsview-sys
      via: adapter or dependency boundary prepared for native integration
---

<objective>
Create the Rust workspace skeleton for DSView CLI so later plans can add native integration without mixing CLI UX, orchestration logic, and unsafe FFI concerns.
</objective>

<scope>
This plan creates the Rust project structure only. It does not yet prove native linking or hardware behavior.
</scope>

<task type="auto">
  <name>Create workspace manifest and crate layout</name>
  <read_first>
    - .planning/PROJECT.md
    - .planning/ROADMAP.md
    - .planning/phases/01-native-integration-foundation/01-RESEARCH.md
    - CLAUDE.md
  </read_first>
  <files>cargo workspace root, crates/dsview-cli, crates/dsview-core, crates/dsview-sys</files>
  <action>Create a root `Cargo.toml` workspace with members `crates/dsview-cli`, `crates/dsview-core`, and `crates/dsview-sys`. Create those three crates on disk. Name packages `dsview-cli`, `dsview-core`, and `dsview-sys`. Configure `dsview-cli` as a binary crate, `dsview-core` and `dsview-sys` as library crates.</action>
  <acceptance_criteria>
    - `Cargo.toml` contains `[workspace]`
    - `Cargo.toml` lists `crates/dsview-cli`, `crates/dsview-core`, and `crates/dsview-sys` under `members`
    - `crates/dsview-cli/Cargo.toml` contains `name = "dsview-cli"`
    - `crates/dsview-core/Cargo.toml` contains `name = "dsview-core"`
    - `crates/dsview-sys/Cargo.toml` contains `name = "dsview-sys"`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Establish crate ownership boundaries</name>
  <read_first>
    - crates/dsview-cli/Cargo.toml
    - crates/dsview-cli/src/main.rs
    - crates/dsview-core/Cargo.toml
    - crates/dsview-core/src/lib.rs
    - crates/dsview-sys/Cargo.toml
    - crates/dsview-sys/src/lib.rs
    - .planning/phases/01-native-integration-foundation/01-RESEARCH.md
  </read_first>
  <files>crates/dsview-cli/src/main.rs, crates/dsview-core/src/lib.rs, crates/dsview-sys/src/lib.rs</files>
  <action>Add minimal source files that encode ownership clearly: `dsview-cli` contains the binary entry point and no unsafe code; `dsview-core` exposes a safe placeholder API for orchestration; `dsview-sys` contains the native integration placeholder module and is documented in code comments as the only allowed home for unsafe FFI. Add dependencies so `dsview-cli` depends on `dsview-core`, and `dsview-core` is prepared to depend on `dsview-sys` only through a narrow boundary.</action>
  <acceptance_criteria>
    - `crates/dsview-cli/src/main.rs` contains a compileable `fn main()`
    - `crates/dsview-core/src/lib.rs` contains a public API item for later orchestration use
    - `crates/dsview-sys/src/lib.rs` contains a module-level note that native FFI belongs there
    - `crates/dsview-cli/Cargo.toml` contains a dependency on `dsview-core`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Verify workspace build and boundary assumptions</name>
  <read_first>
    - Cargo.toml
    - crates/dsview-cli/Cargo.toml
    - crates/dsview-core/Cargo.toml
    - crates/dsview-sys/Cargo.toml
  </read_first>
  <files>Cargo.toml and all crate manifests</files>
  <action>Run `cargo build` from the repository root. Fix any manifest or dependency issues until the Rust workspace builds successfully without attempting to compile or link DSView GUI code. Keep the sys crate as a placeholder boundary only in this plan.</action>
  <acceptance_criteria>
    - `cargo build` exits with status 0
    - build output shows the Rust workspace crates and no Qt/DSView GUI target compilation
  </acceptance_criteria>
</task>

<verification>
- Run `cargo build`
- Read each generated `Cargo.toml`
- Confirm the CLI/core/sys split matches the research recommendation
</verification>
